# Registro decisioni

La decisione approvata più recente e applicabile prevale. Appendere; non riscrivere la storia. Una proposta non è approvata finché l’utente/admin non la accetta esplicitamente.

## Template

### ADR-YYYY-NNN — Titolo

- Data: YYYY-MM-DD
- Stato: proposed | approved | superseded | rejected
- Supersedes: ADR/fonte, se applicabile
- Scope: auction | scoring | data | modeling | architecture | UX | operations
- Decisione: frase non ambigua
- Rationale: motivo
- Conseguenze: comportamento, migration, test, docs
- Approvato da: nome/ruolo

## Decisioni registrate

### ADR-2026-001 — Quattro giri sostituiscono le tre fasi storiche

- Data: 2026-08-10
- Stato: approved
- Supersedes: descrizione d’asta a tre fasi in `docs/archive/Regolamento_originale.txt`
- Scope: auction
- Decisione: G1 busta chiusa per blocchi portieri e top 60 difensori con 200; G2 busta chiusa per top 60 centrocampisti e top 40 attaccanti con residuo G1 +100; G3 asta aperta sui rimanenti con residuo G2 +40; G4 asta aperta sui rimanenti con residuo G3.
- Rationale: è la specifica di lavoro più recente.
- Conseguenze: configurazione versionata; nessuna logica corrente deriva automaticamente dalle tre fasi storiche.
- Approvato da: project owner

### ADR-2026-002 — Liste ufficiali separate dal ranking

- Data: 2026-08-10
- Stato: approved
- Scope: auction | UX
- Decisione: gestire `unknown`, `provisional`, `official`; solo l’import admin `official` vincola l’asta reale.
- Rationale: la composizione top non è ancora nota e può divergere dalla valutazione tecnica.
- Conseguenze: badge non ufficiale, scenari/probabilità di inclusione, snapshot e ricalcolo all’import.
- Approvato da: project owner

### ADR-2026-003 — Modello generativo probabilistico

- Data: 2026-08-10
- Stato: approved
- Scope: modeling
- Decisione: prevedere minuti/stato, voto, eventi, scoreline e dipendenze; applicare il regolamento con Monte Carlo, non prevedere direttamente una fantamedia unica.
- Rationale: bonus individuali, collettivi e non lineari richiedono distribuzioni congiunte.
- Conseguenze: output con quantili/calibrazione e motore deterministico separato.
- Approvato da: project owner

### ADR-2026-004 — UX attorno alle scelte dell’utente

- Data: 2026-08-10
- Stato: approved
- Scope: UX | auction
- Decisione: visualizzare campo/rosa durante la composizione; supportare giocatori bloccati, modulo fisso/libero e ottimizzazione di undici o rosa senza rimuovere lock incompatibili silenziosamente.
- Rationale: l’app deve costruire una soluzione attorno alle preferenze reali dell’utente.
- Conseguenze: infeasibility explanation, distinzione acquistati/ipotetici e profili di rischio.
- Approvato da: project owner

### ADR-2026-005 — Ledger mercato e apprendimento incrementale

- Data: 2026-08-10
- Stato: approved
- Scope: auction | modeling | UX
- Decisione: ogni prezzo/assegnazione osservato aggiorna crediti esatti, slot, inflazione, distribuzioni prezzo e domanda avversaria; undo/correzione ricostruisce tutto dal ledger.
- Rationale: il mercato osservato contiene informazione sul valore residuo e sul comportamento degli avversari.
- Conseguenze: forte shrinkage iniziale, confidenza visibile e separazione tra stato certo e profilo predittivo.
- Approvato da: project owner

### ADR-2026-006 — Workflow LLM selettivo

- Data: 2026-08-10
- Stato: approved
- Scope: architecture | operations
- Decisione: Claude Sonnet lead; Opus solo eccezionale; Haiku per subtask isolati; Gemini CLI per draft low-risk tramite wrapper; niente team permanenti; calcoli di dominio in codice.
- Rationale: minimizzare token Claude a pagamento preservando qualità e responsabilità finale.
- Conseguenze: allowlist, review e test obbligatori per output esterni.
- Approvato da: project owner

### ADR-2026-007 — Policy dati privata e tracciabile

- Data: 2026-08-10
- Stato: approved
- Scope: data | operations
- Decisione: progetto locale/non commerciale; import manuali posseduti ammessi con provenance/tier; niente scraping Fantacalcio, endpoint privati, bypass o redistribuzione.
- Rationale: pragmaticità dell’MVP senza confondere accessibilità e licenza.
- Conseguenze: registry fonti, raw immutabile, moduli rimovibili e degradazione controllata.
- Approvato da: project owner

### ADR-2026-008 — Stack tecnico

- Data: 2026-08-10
- Stato: approved
- Scope: architecture
- Decisione: Python come linguaggio unico per engine deterministico, pipeline dati e modeling. Persistenza locale su DuckDB (analitica/features) + SQLite (stato transazionale: ledger, rose, sessioni) invece di un DB server. UI come app locale monoprocesso in Streamlit per l'MVP (P0/P1 di `UX_PRODUCT.md`); rivalutare un frontend dedicato (es. FastAPI + SPA) solo se il cockpit live richiede interattività che Streamlit non regge, con ADR successiva. Test con `pytest`. Ambiente gestito con `venv` + `pip-tools` o `uv`, senza servizi esterni per la modalità offline-capable.
- Rationale: locale-first e riproducibilità richiedono la minima superficie di infrastruttura; un solo linguaggio evita adapter tra motore e modello statistico; DuckDB è adatto ad analisi colonnari su snapshot immutabili, SQLite è adatto a scritture transazionali frequenti del ledger d'asta live.
- Conseguenze: `docs/PROJECT_SPEC.md` e `docs/OPEN_QUESTIONS.md` aggiornati; M0 di `docs/ROADMAP.md` può iniziare; import/export restano JSON/CSV come già specificato in `UX_PRODUCT.md`.
- Approvato da: project owner

### ADR-2026-009 — Fonti calendario/risultati confermate per M1 (parziale)

- Data: 2026-08-10
- Stato: approved
- Scope: data
- Decisione: football-data.co.uk e OpenFootball (`openfootball/football.json`) sono confermate come fonti di produzione per calendario/risultati/quote storiche, con automazione permessa. Nessuna decisione ancora su lineup/minuti/eventi player-level (Sportmonks vs API-Football): quell'audit resta bloccato in attesa di account/trial, che l'agente non può creare autonomamente (vedi vincoli su creazione account).
- Rationale: entrambe le fonti sono gratuite, senza autenticazione, con licenza/ToS compatibili con l'uso previsto; l'audit del campione 2025/26 (380 partite ciascuna) mostra 0% missing sui campi chiave e 100% di match rate incrociato sulle partite con identità squadra risolta.
- Conseguenze: `docs/SOURCE_REGISTER.md` aggiornato con stato verificato; pipeline di ingestion (`src/fantacalcio/ingest/`) e report riproducibile (`scripts/run_m1_audit.py` → `data/outputs/m1_data_quality_report.md`) disponibili. M1 resta aperto per la parte lineup/minuti/eventi e per l'entity resolution completa (6/20 squadre in coda di revisione manuale, vedi `data/identity/team_review_queue.json`).
- Approvato da: project owner

### ADR-2026-010 — Provider lineup/minuti/eventi: API-Football primario (futuro, a pagamento), StatsBomb come benchmark

- Data: 2026-08-10
- Stato: approved
- Supersedes: parte "lineup/minuti/eventi" di ADR-2026-009 (che la lasciava bloccata)
- Scope: data
- Decisione: **API-Football** è il candidato primario per lineup/minuti/eventi player-level, da attivare su piano a pagamento quando si vorrà coprire la stagione corrente (piano gratuito verificato: 100 richieste/giorno + rate limit per-minuto più stretto, stagioni disponibili solo 2022-2024). **StatsBomb Open Data** resta un benchmark di validazione permanente e gratuito (nessun account, nessun rate limit, ma copertura Serie A ferma al 2015/16) — non un provider di produzione. **Sportmonks è escluso**: il piano gratuito non include la Serie A, nessuna base per preferirlo ad API-Football finché non si valuta un piano a pagamento head-to-head.
- Rationale: audit comparativo eseguito con campioni reali (API-Football stagione 2023, 380 fixture + 30 partite di profondità; StatsBomb stagione 2015/16, 380 partite + 15 di profondità), entrambi validati contro football-data.co.uk sulla stessa stagione con 100% match rate. Nessuna criticità di qualità dati emersa su nessuno dei due; il fattore decisivo è la copertura di lega/stagione, non la qualità dei dati in sé.
- Conseguenze: `docs/SOURCE_REGISTER.md` aggiornato; report completo in `data/outputs/m1_provider_audit_report.md`; `src/fantacalcio/ingest/{api_football,statsbomb}.py` disponibili e testati. Attivazione in produzione di API-Football rimandata a quando si approva il piano a pagamento — nessun'azione ulteriore richiesta per sbloccare M2 (baseline predittive), che può usare StatsBomb come dataset di validazione nel frattempo.
- Approvato da: project owner

### ADR-2026-011 — Modelli forza-squadra Elo e Dixon-Coles confermati come baseline M2

- Data: 2026-08-10
- Stato: approved
- Scope: modeling
- Decisione: adottare Elo sequenziale (rating diff + modello di probabilità esito fittato via log loss) e Dixon-Coles (Poisson attacco/difesa con time-decay, senza la correzione tau per i punteggi bassi — omissione dichiarata, non silenziosa) come modelli di forza-squadra per M2, entrambi validati rolling-origin su 5 stagioni (2021/22-2025/26) di football-data.co.uk.
- Rationale: backtest su 4 fold espandenti mostra che entrambi i modelli battono le baseline naive (tasso esito costante, media gol) su ogni fold: log loss medio 1.087 (baseline) → 1.008 (Elo) / 1.010 (Dixon-Coles); MAE gol medio 0.935 (baseline) → 0.870 (Dixon-Coles).
- Conseguenze: `src/fantacalcio/modeling/{validation,baselines,elo,dixon_coles}.py` disponibili e testati (98 test totali); report riproducibile in `data/outputs/m2_team_strength_backtest.md`. Squadre neopromosse senza storico ricevono forza media di default (flag `is_known_team`/colonna "unknown-team matches" nel report) — comportamento dichiarato, non un bug. Correzione tau di Dixon-Coles e time-decay tuning restano miglioramenti futuri, non bloccanti per procedere al blocco successivo di M2 (modello voto/minuti giocatore).
- Approvato da: project owner

### ADR-2026-012 — Stimatore voto giocatore a shrinkage, con limite di metodo dichiarato

- Data: 2026-08-10
- Stato: approved
- Scope: modeling
- Decisione: adottare uno stimatore Empirical-Bayes (media giocatore pesata verso media di ruolo, `weight = n/(n+prior_games)`, `prior_games=60`) come baseline per il voto base giocatore, validato walk-forward su 5 stagioni reali (59.306 righe votate, pannello "Fantacalcio"). Il modello di partecipazione/minutaggio resta esplicitamente fuori scope: i file voti elencano solo i giocatori votati, non l'intera rosa, quindi non è derivabile "probabilità di essere schierato" da questi dati soli.
- Rationale: lo stimatore batte tutte le baseline obbligatorie sull'MAE complessivo (0.4137 vs 0.4154 media-ruolo, 0.4494 media-stagione, 0.5670 ultimo voto noto). **Limite dichiarato**: `prior_games=60` è stato scelto scansionando un set di valori sullo stesso backtest poi usato per il report finale (nessun validation split separato per la selezione dell'iperparametro) — è una forma mite di tuning-on-test. Il margine di miglioramento sulla baseline è inoltre piccolo (~0.4%) e non uniforme: sul ruolo C lo stimatore è leggermente peggiore della media-ruolo (0.3805 vs 0.3760); sugli altri ruoli (P, D, A) è marginalmente migliore. Non è un risultato clamoroso, è un miglioramento modesto e onestamente riportato.
- Conseguenze: `src/fantacalcio/modeling/player_voto.py` disponibile e testato (111 test totali nel progetto); report in `data/staged/fantacalcio_voti_manual/_m2_player_voto_backtest.md` (locale, non committato, deriva da dati a licenza personale). Raffinamento futuro suggerito: nested cross-validation per la scelta di `prior_games` invece dello sweep singolo attuale, prima di usare questo stimatore per decisioni economiche reali (offerte d'asta).
- Approvato da: project owner

### ADR-2026-013 — Meccaniche d'asta confermate dal recap admin; corretto errore G3/G4

- Data: 2026-08-11
- Stato: approved
- Supersedes: parte "meccaniche" di ADR-2026-003/ADR-2026-009 (il premesso quattro-giri e i budget restano confermati, invariati)
- Scope: auction
- Decisione: G1/G2 sono a busta chiusa con liste (6 preferenze/lista, priorità preferenza poi offerta, fallback ad assegnazione al prezzo minimo se nessuna preferenza vince, minimo d'offerta dalla lista pubblicata). G3/G4 sono a busta chiusa libera (nessuna lista/preferenza/vincolo di ruolo, vince l'offerta più alta, max 6 giocatori a fase, minimo d'offerta = quotazione giocatore) — **non asta aperta/live come erroneamente configurato in precedenza**. Una fase 3 finale, non un'asta, assegna manualmente i rimanenti partendo dalle squadre con più crediti residui.
- Rationale: recap completo fornito direttamente dall'admin (`docs/archive/Recap_regole_asta_admin_20260811.txt`), fonte primaria autorevole. La correzione G3/G4 emerge da lettura diretta del testo: non descrive mai un'asta dal vivo, solo "offerta più alta" in busta chiusa.
- Conseguenze: `config/auction_rules.v1.yaml` aggiornato (mode `sealed_bid_list`/`sealed_bid_free` invece di `sealed_bid`/`open_auction`; nuova sezione `post_auction_completion`); `src/fantacalcio/config.py` aggiornato di conseguenza; `docs/AUCTION_RULES.md` e `docs/OPEN_QUESTIONS.md` aggiornati. Restano aperti: tie-breaker per pari preferenza+pari offerta, soglie esatte delle liste (arrivano venerdì sera), formato file admin da importare. `resolve_sealed_bid_round()` in `src/fantacalcio/domain.py` resta bloccata (`NotImplementedError`) — le meccaniche sono ora note ma l'implementazione è lavoro futuro (M3), non fatto in questa ADR. 117 test passano.
- Approvato da: project owner

### ADR-2026-014 — Tasso di partecipazione stagionale come feature, derivato dai voti

- Data: 2026-08-11
- Stato: approved
- Scope: modeling
- Decisione: calcolare il tasso di partecipazione (giornate votate / 38) per giocatore-stagione direttamente dal pannello voti (tutte e 5 le stagioni), invece di dipendere solo dal campo `Pv` del file statistiche (che copre una sola stagione). Il file statistiche resta utile come **cross-check indipendente**, non come unica fonte.
- Rationale: la stagione 2025/26 nei voti è già completa (38/38, noto da M1), quindi non è un problema di walk-forward infra-stagionale ma di persistenza stagione-su-stagione — utile proprio per preparare l'asta 2026/27 che non è ancora iniziata. Risultati su dati reali (2955 coppie giocatore-stagione, 1432 transizioni stagione-su-stagione consecutive): il tasso dell'anno precedente predice quello dell'anno successivo meglio della media globale (MAE 0.2173 vs 0.2560, correlazione 0.428) — segnale più solido del guadagno marginale trovato per il voto base (ADR-2026-012). Cross-check contro `Pv`: correlazione 0.979, MAE 2 giornate su 38 — le due fonti indipendenti (voti, statistiche) sono coerenti tra loro.
- Conseguenze: `src/fantacalcio/modeling/participation.py` disponibile e testato (124 test totali nel progetto); report in `data/staged/fantacalcio_voti_manual/_m2_participation_report.md` (locale, non committato). Non ancora integrato come feature diretta nello stimatore voto di `player_voto.py` — prossimo passo naturale se si vuole un output combinato voto-atteso × probabilità-di-schierarsi.
- Approvato da: project owner

### ADR-2026-015 — Prima valutazione pre-asta sul roster 2026/27 (voto atteso + partecipazione)

- Data: 2026-08-11
- Stato: approved
- Scope: modeling
- Decisione: combinare lo stimatore voto (ADR-2026-012, fittato su tutto lo storico 2021/22-2025/26, non walk-forward perché la 2026/27 non è ancora giocata) e il tasso di partecipazione (ADR-2026-014, ultima stagione nota) applicati al listone reale 2026/27 (498 giocatori). Output esplicitamente dichiarato **non un modello di valore d'asta**: manca replacement level, scarsità, budget, domanda avversari (layer forecast-to-bid di M3/`docs/DATA_AND_MODELING.md`).
- Rationale: prima verifica end-to-end su dati reali dell'intera pipeline costruita finora (ingestion → identity → modeling). Risultati plausibili: i primi 15 per voto atteso sono nomi noti di alto livello (Berardi, Lautaro Martinez, Dybala, Calhanoglu, Thuram, migliori portieri); media per ruolo coerente (portieri 6.175, attaccanti/centrocampisti ~6.0-6.06, difensori 5.930 — sensato, il voto base non include ancora il modificatore difesa che si applica nel motore di scoring). 84/498 giocatori (16.9%) sono nuovi al dataset a 5 stagioni (neopromossi/nuovi acquisti) e ricevono fallback a media di ruolo, segnalato esplicitamente riga per riga, non nascosto.
- Conseguenze: `src/fantacalcio/modeling/player_voto.py` esteso con `fit_final_stats` (stato finale su tutto lo storico, distinto da `walk_forward` che è per backtest); `participation.py` esteso con `latest_known_participation`. Script `scripts/run_m2_pre_auction_valuations.py`, output in `data/staged/fantacalcio_voti_manual/_m2_pre_auction_valuations.{md,csv}` (locale). 129 test passano.
- Approvato da: project owner

### ADR-2026-016 — Motore deterministico di scoring: componenti individuali confermate, resto bloccato

- Data: 2026-08-11
- Stato: approved
- Scope: modeling | auction
- Decisione: `src/fantacalcio/scoring/engine.py` implementa solo le componenti individuali confermate in `docs/SCORING_RULES.md` e calcolabili con i dati disponibili: gol (+3), assist (+1, **approssimazione dichiarata**: i dati non distinguono assist da assist-light, tutti scored a tariffa piena), gol subito (-1, solo portiere — vedi correzione sotto), porta inviolata (+1, **solo portiere**), rigore sbagliato (-3), autogol (-2), cartellini (-0,5/-1), assente = 0. Bloccati esplicitamente (`ScoringComponentBlocked`, mai un valore inventato): rigore parato/procurato (regola non confermata, pur avendo il dato), gol pareggio/vittoria e bonus capitano (dato mancante), fair play/modificatore difesa/bonus rendimento/bonus inferiorità (formula non confermata in `OPEN_QUESTIONS.md`).
- Rationale: verifica su dati reali (59.306 righe) ha trovato un bug reale prima del rilascio: il campo `goals_conceded` dei voti è popolato quasi solo per i portieri (71% non-zero) ed è sostanzialmente sempre 0 per i difensori (0,005% non-zero) — non perché tengano la porta inviolata, ma perché il dato individuale non è tracciato per quel ruolo in questa fonte. Applicare "porta inviolata" anche ai difensori con quel campo li premiava quasi ogni giornata, gonfiando il punteggio (correlazione con `Fm` reale 0,33-0,44, scarto medio -0,51/-0,59). Corretto limitando la porta inviolata al solo portiere: correlazione sale a 0,47-0,57, scarto scende a -0,15/-0,23. Un vero bonus porta-inviolata per i difensori richiederebbe un join con i risultati partita (football-data.co.uk, non ancora collegato).
- Conseguenze: `scripts/run_scoring_engine_validation.py` confronta la fantavoto calcolata con `Fm` reale su tutte le 5 stagioni, riportando onestamente il divario residuo (dovuto alle componenti ancora escluse) invece di nasconderlo. 153 test passano. Prossimo passo naturale: join coi risultati partita per porta-inviolata-difensori e modificatore difesa; poi Monte Carlo per giornate future.
- Approvato da: project owner

### ADR-2026-017 — Join risultati partita per porta-inviolata: aiuta il portiere, NON i difensori

- Data: 2026-08-11
- Stato: approved
- Scope: modeling
- Decisione: `src/fantacalcio/modeling/team_matchday.py` deriva gol fatti/subiti per squadra-giornata da football-data.co.uk (giornata dedotta dal rank cronologico delle partite per squadra, verificato contro il campo "round" esplicito di OpenFootball per la stagione 2025/26 — coincide esattamente). Il motore di scoring usa questo dato per portiere/porta-inviolata quando disponibile (96,6% di copertura, più completo del campo individuale), **ma il tentativo di estenderlo ai difensori è stato testato e scartato**: peggiorava nettamente l'accordo con `Fm` reale (correlazione da ~0,5 a 0,16-0,41, scarto da negativo piccolo a +0,75/+0,99). Conferma empirica che questa lega non applica un malus individuale "gol subito" al singolo difensore — il contributo difensivo passa dal modificatore squadra (ancora bloccato), non da un malus per-giocatore.
- Rationale: la scoperta non era assumibile a priori (il testo di `SCORING_RULES.md` non specifica il ruolo per "gol subito"/"porta inviolata"); il test contro dati reali ha falsificato l'ipotesi iniziale ed è stato onestamente ribaltato invece di forzare il risultato atteso. Risultato finale: correlazione con `Fm` reale migliorata a 0,51-0,60 (da 0,47-0,57 della versione solo-portiere-dato-individuale), grazie alla maggiore copertura del join rispetto al campo individuale.
- Conseguenze: `src/fantacalcio/scoring/engine.py` aggiornato con nota esplicita "Tested-and-reverted" nel docstring, per evitare che un futuro tentativo ripeta lo stesso errore senza sapere che è già stato provato. 162 test passano. Join squadra-giornata riutilizzabile in futuro per il modificatore difesa (quando la formula sarà confermata).
- Approvato da: project owner

### ADR-2026-018 — Distribuzione Monte Carlo del fantavoto via bootstrap, non stima puntuale

- Data: 2026-08-11
- Stato: approved
- Scope: modeling
- Decisione: `src/fantacalcio/scoring/monte_carlo.py` sostituisce/arricchisce la stima puntuale del voto (ADR-2026-012/2026-015) con una distribuzione (media, mediana, P10-P90) ottenuta ricampionando **righe storiche reali complete** (voto + eventi individuali + `team_goals_conceded`), non assumendo una forma parametrica (normale/Poisson) mai confermata dai dati. Mistura a shrinkage: pesca dalla storia del giocatore con probabilità `n/(n+60)`, altrimenti dal pool di ruolo — stesso schema già validato per lo stimatore voto puntuale. Seed fissato (42), riproducibile.
- Rationale: `CLAUDE.md` vieta output puntuali per le previsioni ("distributions, not single magic numbers"); questo chiude quel gap. Validazione walk-forward onesta (addestrato su 2021/22-2024/25, testato su 2025/26 mai visto): correlazione con `Fm` reale **0,3532** — sensibilmente più bassa della validazione in-sample dell'ADR-2026-017 (0,51-0,60), riportato senza minimizzare. Due possibili cause non discriminabili con i dati attuali: (1) prevedere una stagione futura è oggettivamente più difficile che spiegarla con i suoi stessi dati; (2) il 31,4% dei giocatori target non ha storico pre-2025/26 (trasferimenti/promozioni) e viene scored solo dalla media di ruolo, che non cattura abilità individuale.
- Conseguenze: applicato al roster reale 2026/27 (498 giocatori, 1000 simulazioni ciascuno) in `data/staged/fantacalcio_voti_manual/_monte_carlo_2026_27.{csv,md}` (locale). Report per ruolo mostra ampiezza di incertezza (P90-P10) molto diversa: attaccanti/portieri ~4,5 punti di spread, centrocampisti/difensori ~1,6-2,3 — segnale plausibile (ruoli offensivi/portiere più volatili). 171 test passano. Correlazione 0,35 va trattata come l'aspettativa onesta per previsioni out-of-sample con questo metodo, non il numero 0,5-0,6 in-sample.
- Approvato da: project owner

### ADR-2026-019 — Primo blocco M3: valore sopra il replacement (VAR)

- Data: 2026-08-11
- Stato: approved
- Scope: auction | modeling
- Decisione: `src/fantacalcio/auction/replacement.py` calcola il livello di replacement per ruolo (fantavoto medio simulato del giocatore all'N-esimo posto, N = slot totali di lega letti da `config/auction_rules.v1.yaml`, mai hardcoded) e il VAR (valore sopra replacement) per ogni giocatore, con range di incertezza propagato (P10/P90), non un solo numero.
- Rationale: applicato al roster reale 2026/27 ha rivelato una carenza di offerta reale non ipotizzata a priori: **88 attaccanti disponibili su 100 slot di lega richiesti** (-12) e 59 portieri su 60 (-1). Per gli attaccanti questo è esattamente lo scenario per cui esiste `forwards_fallback_if_supply_insufficient: 4` nella config — non un errore del codice, un fatto del mercato che il codice ora segnala esplicitamente invece di assorbirlo silenziosamente nel fallback "usa l'ultimo disponibile". I risultati sono plausibili: i migliori attaccanti (Lautaro Martinez, Thuram) hanno il VAR più alto, coerente con l'intuito calcistico.
- Conseguenze: `scripts/run_m3_replacement_values.py` applicato ai dati reali, report in `data/staged/fantacalcio_voti_manual/_m3_replacement_values.{csv,md}` (locale). 178 test passano. Resta fuori scope: scarsità dinamica durante l'asta live (serve il ledger), fit con la rosa dell'utente, budget shadow price, domanda avversari — prossimi blocchi M3.
- Approvato da: project owner

### ADR-2026-020 — Flag esplicito di affidabilità dati; ricerca campionati esteri scoperta ma non integrata

- Data: 2026-08-11
- Stato: approved
- Scope: modeling | data
- Decisione: `src/fantacalcio/modeling/data_quality.py` classifica ogni giocatore in 4 livelli (`full_history`, `partial_history`, `no_history_transfer`, `no_history_new_team`) invece di trattare "zero storico" come un unico bucket indifferenziato. Nessuna distribuzione viene allargata artificialmente per compensare — il flag serve a segnalare all'utente dove il prior a media di ruolo è debole (trasferimento vero) invece di ragionevole (giocatore di una neopromossa), non a inventare un'incertezza numerica senza base.
- Rationale: su richiesta esplicita di arricchire i profili con statistiche da campionati esteri per giocatori a zero/pochi dati. Ricerca: **StatsBomb Open Data copre anche Premier League, Bundesliga, La Liga, Ligue 1** (gratis, nessun account, già verificato in M1) — ma la copertura recente è limitata (Bundesliga 2023/24 è la più recente; Premier League ferma al 2015/16) e costruire l'ingestion multi-lega con risoluzione identità cross-campionato è un blocco di dimensioni paragonabili a un intero M1, non improvvisabile in questa sessione. **Non implementato ora, documentato come pista futura scoperta e pronta**. Nel frattempo, bug reale trovato e corretto durante l'implementazione del flag: la classificazione confrontava lo zero-storico contro l'unione di 5 stagioni di squadre invece che contro la sola stagione più recente (2025/26) — poiché la Serie A ruota per promozione/retrocessione, una squadra neopromossa può comunque essere apparsa in una stagione passata, e il confronto sbagliato classificava **tutti** gli 84 giocatori come "vero trasferimento" invece di distinguerli (corretto: 50 trasferimenti reali, es. Stones/Mastantuono, contro 34 giocatori di neopromosse Frosinone/Venezia).
- Conseguenze: `scripts/run_m3_replacement_values.py` mostra il breakdown per livello e una lista dedicata dei 50 "trasferimento reale" con nota di cautela esplicita. 184 test passano. `docs/SOURCE_REGISTER.md` da aggiornare con la pista StatsBomb multi-lega per un blocco futuro dedicato.
- Approvato da: project owner

### ADR-2026-021 — Assegnazione ai pool G1-G4 reali, ranking provvisorio non ufficiale

- Data: 2026-08-11
- Stato: approved
- Scope: auction
- Decisione: `src/fantacalcio/auction/round_pools.py` assegna ogni giocatore al pool reale (G1: blocco portieri + top 60 difensori; G2: top 60 centrocampisti + top 40 attaccanti; G3/G4: tutti i rimanenti) usando il VAR come criterio di ranking. Le soglie numeriche (60/60/40) sono lette dai nomi dei pool già presenti in `config/auction_rules.v1.yaml`, non ridichiarate come letterali nel codice. Il risultato è marcato esplicitamente `list_state=provisional` (mai `official`) — distinto dalla lista vera dell'admin, che arriva via Google Form venerdì sera (`docs/archive/Recap_regole_asta_admin_20260811.txt`).
- Rationale: le soglie esatte di pareggio non hanno una regola di tie-break confermata (`docs/OPEN_QUESTIONS.md`); invece di inventarne una, tutti i giocatori a pari valore al cutoff vengono inclusi nel pool superiore (verificato: 61 difensori in G1 invece di esattamente 60, per un pareggio al 60° posto). Applicato al roster 2026/27 reale: 59 portieri, 61 difensori, 60 centrocampisti, 40 attaccanti nei pool G1/G2, 278 giocatori in G3/G4.
- Conseguenze: `scripts/run_m3_replacement_values.py` mostra la ripartizione per pool e il round di ogni giocatore nella classifica VAR. 191 test passano. Prossimo passo naturale: collegare questi pool al ledger d'asta vivo (M0) per un vero motore di raccomandazione round-per-round.
- Approvato da: project owner

### ADR-2026-022 — Raccomandazione di offerta massima, collegata al ledger vivo

- Data: 2026-08-11
- Stato: approved
- Scope: auction
- Decisione: `src/fantacalcio/auction/bid_recommendation.py` calcola un'offerta massima raccomandata usando la formula standard delle aste fantasy ("dollar rule": riserva 1 credito per ogni slot di rosa ancora da riempire tranne quello corrente; il budget discrezionale residuo si distribuisce proporzionalmente al VAR positivo tra i giocatori ancora disponibili nel pool). Budget e slot rimanenti sono letti dal replay del ledger (`src/fantacalcio/domain.py`), mai da uno snapshot statico — la raccomandazione cambia in tempo reale con ciò che viene assegnato, sia dagli avversari sia dalla propria squadra.
- Rationale: dichiarata esplicitamente come prima versione semplificata, non il layer forecast-to-bid completo di `docs/DATA_AND_MODELING.md` (mancano domanda avversari modellata, inflazione di mercato osservata, fit di modulo/rischio — tutti richiedono dati che non abbiamo ancora, es. uno storico di aste reali). Bug reale trovato durante i test: gli ID giocatore nel ledger sono stringhe (`AssignmentItem.player_ids: tuple[str,...]`) mentre nel pool VAR sono interi — il confronto silenziosamente non escludeva mai i giocatori già assegnati. Corretto normalizzando a stringa prima del confronto.
- Conseguenze: `scripts/run_m3_bid_recommendation_demo.py` dimostra su dati VAR reali (pool difensori G1 2026/27) che la raccomandazione sale quando i rivali comprano le alternative migliori e scende quando la propria squadra consuma budget/slot — comportamento corretto per uno strumento da usare live in asta. 201 test passano.
- Approvato da: project owner

### ADR-2026-023 — Dixon-Coles collegato al Monte Carlo del voto: aggiustamento piccolo ma reale, adottato con k validato

- Data: 2026-08-11
- Stato: approved
- Scope: modeling
- Decisione: `src/fantacalcio/scoring/team_strength_adjustment.py` collega il modello di forza-squadra Dixon-Coles (validato, ADR-2026-011) — mai riusato dal voto/Monte Carlo prima di questo blocco — alle distribuzioni Monte Carlo (ADR-2026-018). Per ogni giocatore in ruolo offensivo (A/C, sul rating d'attacco) o difensivo (D, sul rating di difesa — P escluso, già coperto da `team_goals_conceded` via ADR-2026-017), calcola lo scarto tra il rating Dixon-Coles della squadra attuale e la media storica pesata per partite delle squadre in cui il giocatore ha giocato, e applica un aggiustamento additivo `k * scarto` ai campioni Monte Carlo. Convenzione di segno verificata esplicitamente in test (rating difesa più basso = difesa più forte, l'opposto dell'attacco).
- Rationale: coefficiente `k` scelto per validazione onesta via walk-forward (`scripts/run_team_strength_adjustment_validation.py`, stesso schema di `prior_games` in ADR-2026-012), non arbitrario: addestrato su 2021/22-2024/25, testato su 2025/26 mai visto, sweep di `k` ∈ {0, 0.1, 0.25, 0.5, 1.0}. Risultato: correlazione con `Fm` reale sale da 0,3472 (k=0, baseline) a 0,3522 (k=0,5) con un pattern monotono (0→0,25→0,5 in salita, poi discesa a k=1,0) coerente con un segnale reale e non rumore campionario — miglioramento piccolo (+0,005, ~1,4% relativo) ma non nullo, adottato con `k=0,5`. A differenza del tentativo sui difensori (ADR-2026-017), qui il test conferma l'ipotesi invece di falsificarla, ma la dimensione dell'effetto è riportata senza gonfiarla: non è un miglioramento drammatico.
- Conseguenze: `scripts/run_monte_carlo_fantavoto.py` (parte B, applicazione al roster reale 2026/27) ora fitta Dixon-Coles su tutte le 5 stagioni e applica l'aggiustamento `k=0,5` ai 498 giocatori; colonna `team_strength_adjustment` aggiunta al CSV di output. La parte A (validazione walk-forward contro `Fm` reale) resta senza aggiustamento per isolare il baseline storico. 209 test passano (8 nuovi per `team_strength_adjustment.py`). Report di validazione dedicato in `data/staged/fantacalcio_voti_manual/_team_strength_adjustment_validation.md` (locale).
- Approvato da: project owner

### ADR-2026-024 — FVM come prior secondario per giocatori a basso/zero storico: miglioramento più forte del blocco 1, adottato

- Data: 2026-08-11
- Stato: approved
- Scope: modeling
- Decisione: `src/fantacalcio/scoring/fvm_prior.py` sostituisce, solo per giocatori con meno di 10 partite di storico proprio, il fallback "pool di ruolo piatto" (media di tutti i centrocampisti mai visti, indifferentemente) di `monte_carlo.py` con un pool segmentato per quartile di FVM (`fvm_classic`, già ingerito dalle quotazioni, mai usato prima d'ora) — un giocatore a basso storico pesca da giocatori storicamente valutati in modo simile dal mercato Fantacalcio stesso, non dalla media indifferenziata di ruolo. I bordi dei quartili sono fittati sui soli dati di training (stesso schema walk-forward degli altri blocchi), non sui dati del giocatore target.
- Rationale: validato onestamente su un sotto-insieme mirato (`scripts/run_fvm_prior_validation.py`), non sull'intero roster — confrontare su tutti i giocatori avrebbe diluito l'effetto, dato che riguarda solo chi ha poco/nessuno storico. Addestrato su 2021/22-2024/25, testato sui 177 giocatori 2025/26 con meno di 10 partite pre-stagione e FVM noto: correlazione con `Fm` reale sale da 0,3326 (pool piatto) a 0,4048 (pool per FVM) — un salto di +0,072, molto più marcato del +0,005 del blocco 1 (ADR-2026-023) e non un artefatto di soglia arbitraria (lo scarto medio assoluto scende in parallelo, da 0,6395 a 0,5645).
- Conseguenze: `scripts/run_monte_carlo_fantavoto.py` (parte B) applica il prior FVM a 130 dei 498 giocatori del roster 2026/27 (quelli con <10 partite di storico e FVM noto); colonna `used_fvm_prior` aggiunta al CSV. Verifica di plausibilità: due trasferimenti a zero storico Serie A ma FVM alto (Ramos G., Adams A.) ora entrano nella top-15 per fantavoto simulato, invece di essere sepolti nella media di ruolo come accadeva prima — comportamento atteso, non più un artefatto della carenza dati. 216 test passano (7 nuovi per `fvm_prior.py`). Report di validazione dedicato in `data/staged/fantacalcio_voti_manual/_fvm_prior_validation.md` (locale).
- Approvato da: project owner

### ADR-2026-025 — Quote scommesse recuperate come cross-check di Dixon-Coles, non come input di previsione; bug di provenance nello snapshot corretto

- Data: 2026-08-11
- Stato: approved
- Scope: data | modeling
- Decisione: `src/fantacalcio/ingest/football_data_co_uk.py` recupera le colonne quote medie pre-partita (`AvgH`/`AvgD`/`AvgA`, media di mercato tra bookmaker, non un singolo bookmaker) precedentemente scartate da `_OPTIONAL_COLUMNS`. `src/fantacalcio/modeling/market_odds.py` le converte in probabilità implicite (de-vigged, overround rimosso) e in un rating di forza-squadra basato sui punti attesi dal mercato. **Non collegato alla pipeline di previsione 2026/27**: le quote per una stagione non ancora iniziata non esistono, quindi non c'è nulla da recuperare per la stagione target reale — usato solo come cross-check a parità di stagione (`scripts/run_market_odds_crosscheck.py`) contro le forze Dixon-Coles (ADR-2026-011, fittato solo su gol).
- Rationale: il mercato scommesse prezza informazione che Dixon-Coles (solo gol) non vede mai (infortuni, squalifiche, forma, voci) — un forte accordo tra i due è una validazione indipendente rassicurante, non un motivo per sostituire il modello a gol con uno a quote. Risultato: correlazione media 0,9278 su 5 stagioni (range 0,8974-0,9604) tra forza combinata Dixon-Coles (attacco - difesa) e rating di mercato — accordo forte, nessuna bandiera rossa sul modello esistente. Durante il recupero, trovato e corretto un bug reale di provenance in `src/fantacalcio/ingest/snapshot.py`: due snapshot dello stesso source scritti nella stessa cartella con timestamp al secondo (es. un audit M1 che scarica più stagioni in rapida sequenza) condividevano un unico `manifest.json` — la seconda scrittura sovrascriveva silenziosamente il manifest della prima, lasciando il contenuto checksummato corretto ma la provenance del primo file corrotta (verificato: il manifest di `serie_a_2122.csv` in una cartella condivisa puntava in realtà a `serie_a_2223.csv`). Corretto rendendo il nome del manifest specifico per file (`{filename}.manifest.json`).
- Conseguenze: `scripts/reingest_football_data_odds.py` ri-scarica le 5 stagioni con snapshot puliti (non ricostruiti da manifest corrotti) per avere provenance corretta oltre alle nuove colonne quote. 222 test passano (6 nuovi per `market_odds.py`). Report cross-check in `data/staged/fantacalcio_voti_manual/_market_odds_crosscheck.md` (locale). Nessuna modifica alla pipeline di applicazione roster 2026/27 — dichiarato esplicitamente fuori scope per mancanza di dati futuri, non per pigrizia.
- Approvato da: project owner

### ADR-2026-026 — Decadimento temporale nel bootstrap voto/partecipazione: testato onestamente, non adottato

- Data: 2026-08-11
- Stato: approved (tested-and-not-adopted)
- Scope: modeling
- Decisione: `src/fantacalcio/modeling/time_decay.py` (decadimento esponenziale per "giornata globale", coerente con `xi` di Dixon-Coles) collegato in due punti come funzionalità **opt-in**, non attivata di default: (1) `src/fantacalcio/scoring/monte_carlo.py`'s `simulate_fantavoto(..., use_recency_weights=True)` — ricampionamento pesato per recency invece che uniforme, con `HistoricalRow.recency_weight` (default 1.0, comportamento pre-blocco-4 invariato quando non richiesto esplicitamente); (2) `src/fantacalcio/modeling/participation.py`'s `decayed_participation_estimate()` — media multi-stagione pesata per recency, alternativa a `latest_known_participation` (singola stagione più recente).
- Rationale: validato via walk-forward su entrambi i fronti, **nessuno dei due supera onestamente il baseline esistente**. (1) Bootstrap: sweep di `half_life_matchdays` ∈ {19, 38, 76, 150, 300, 1000} mostrava inizialmente un apparente miglioramento (fino a +0,012 di correlazione con `Fm` reale), ma un controllo a parità di algoritmo di campionamento (`rng.choice` con pesi uniformi, cioè decadimento nullo ma stesso percorso di codice del campionamento pesato) ha isolato l'effetto reale: **+0,0168 di correlazione veniva dal solo cambio di algoritmo RNG** (`rng.choice` vs `rng.integers`, che consumano lo stream casuale in modo diverso anche a parità di distribuzione), e il decadimento vero e proprio, controllato per questo artefatto, dava **-0,0035** (leggermente peggio, non meglio). Errore metodologico evitato grazie al controllo esplicito, non assunto. (2) Partecipazione: sweep di `half_life_seasons` ∈ {0.5, 1.0, 2.0, nessuno} contro il baseline "ultima stagione nota" — il miglior caso (`half_life=0,5`, quasi equivalente a "usa quasi solo l'ultima stagione") dava +0,0017 di correlazione, sotto la soglia di rilevanza (0,005) usata in tutti i blocchi precedenti; la correlazione peggiorava monotonicamente allontanandosi da "solo ultima stagione" verso una media multi-stagione piatta (0,4369→0,4386→0,4323→0,4242→0,4116), segno che la partecipazione è abbastanza volatile stagione su stagione da rendere lo storico più vecchio prevalentemente rumore, non segnale aggiuntivo.
- Conseguenze: `scripts/run_time_decay_validation.py` e `scripts/run_participation_decay_validation.py` documentano entrambi gli esiti onestamente (inclusi i numeri "prima del controllo" per trasparenza sul quasi-errore). L'infrastruttura resta nel codice (funzionante, testata, opt-in) per un possibile riuso futuro se arriveranno più stagioni di dati, ma **non è collegata** a `scripts/run_monte_carlo_fantavoto.py` — nessuna modifica al roster 2026/27 applicato. 234 test passano (18 nuovi: 6 `time_decay.py`, 2 `monte_carlo.py`, 4 `participation.py`, più i test di `market_odds.py`/`fvm_prior.py` dei blocchi precedenti già conteggiati). Chiude la sequenza dei 4 blocchi di miglioramento aperta da `docs/CURRENT_TASK.md`.
- Approvato da: project owner

### ADR-2026-027 — M4 slice 1: prima schermata UI, ricerca/scheda giocatore su Streamlit + DuckDB

- Data: 2026-08-11
- Stato: approved
- Scope: architecture | ux
- Decisione: prima riga di codice UI del progetto. `src/fantacalcio/persistence/player_table.py` costruisce una tabella DuckDB locale (`data/local/fantacalcio.duckdb`, gitignored) da `_m3_replacement_values.csv` (già Monte Carlo + VAR + round pool + data quality tier), con tabella `meta` separata per provenance (path sorgente, sha256, timestamp di build) — mai mostrata senza dire quando/da cosa è stata costruita, per la regola `as_of`/provenienza di `CLAUDE.md`. `app/Home.py` (stato dati) e `app/pages/1_Giocatori.py` (ricerca/filtri/scheda) sono sola lettura: nessuna formula viene ricalcolata nella UI, solo lettura di ciò che il motore ha già prodotto. Stack conforme alla decisione già presa in ADR-2026-008 (DuckDB per letture columnar su snapshot immutabili; SQLite per il ledger vivo, riservato a una slice M4 successiva non ancora costruita).
- Rationale: `docs/ROADMAP.md`'s regola di priorità blocca solo il modeling avanzato (M5) prima dei gate; M0-M3 hanno gate soddisfatti (dominio, dati, baseline, motore VAR/max-bid), quindi M4 può iniziare. Scelta la prima fetta di `docs/UX_PRODUCT.md`'s priorità P0 ("ricerca/filtri, scheda compatta") perché è la più piccola unità che rende effettivamente usabile ciò che il motore già calcola, prima dell'asta reale. Campi della "scheda giocatore" non ancora disponibili in questa slice (offerta consigliata, massimo dinamico, valore marginale per la rosa, confronto a tre alternative — tutti richiedono il ledger vivo o il costruttore rosa, non ancora costruiti) sono mostrati esplicitamente come "non ancora disponibile" in un pannello dedicato, mai inventati o impliciti. Il badge `list_state=provisional` (ADR-2026-021) è sempre visibile: la UI non lascia mai intendere che il ranking del modello sia la lista ufficiale dell'admin.
- Conseguenze: verificato end-to-end in browser (non solo test unitari, per la regola di sessione sulle modifiche UI): ricerca per nome, filtro per ruolo/squadra/round pool/qualità dati, scheda dettaglio con driver oggettivi (aggiustamento Dixon-Coles, prior FVM, avviso esplicito per `no_history_transfer`) tutti confermati funzionanti su dati reali (es. "Ramos" → 1 risultato, zero storico, prior FVM applicato, avviso cautela mostrato correttamente). 245 test passano (11 nuovi per il data layer `player_table.py`; la UI Streamlit stessa non è unit-testata, non praticamente possibile — verificata in browser). Aggiunte dipendenze `duckdb`/`streamlit` a `pyproject.toml`, nuova cartella `data/local/` gitignored, `.claude/launch.json` per l'avvio locale (`streamlit run app/Home.py`).
- Approvato da: project owner

### ADR-2026-028 — M4 slice 2: ledger vivo (SQLite) + tabellone squadre + registrazione risultati; bug critico di budget cross-team corretto

- Data: 2026-08-11
- Stato: approved
- Scope: architecture | domain | ux
- Decisione: `src/fantacalcio/persistence/ledger_store.py` collega il ledger deterministico già esistente (`src/fantacalcio/domain.py`, M0) a una tabella SQLite append-only (ADR-2026-008), riusando `src/fantacalcio/ledger_io.py`'s serializzazione (nessuno schema duplicato). Nuova funzione `domain.effective_events()`: `replay()` non annulla retroattivamente gli effetti di un evento voidato/corretto (documentato e testato fin dal M0) — `effective_events()` è la query "stato attuale" che un'interfaccia deve mostrare, distinta dall'audit trail completo. `app/pages/2_Squadre.py` (tabellone 20 squadre, registrazione manuale risultati, undo via `VoidEvent`) e l'estensione di `app/pages/1_Giocatori.py` con un "tetto di riferimento" (massimo consigliato, ADR-2026-022) per la squadra selezionata. **Vincolo di dominio esplicito**: i round G1-G4 sono sealed bid risolti dall'admin, non un'asta live — questa slice registra risultati già decisi, non offerte in tempo reale; il tetto di riferimento è esplicitamente etichettato come tale, non come previsione di vittoria. Identificativi squadra generici (`team_01`..`team_NN`), nessun nome reale non ancora in repo.
- **Bug critico trovato e corretto durante il testing in browser**: `replay()` teneva un unico dizionario `remaining_by_round` condiviso fra TUTTE le squadre invece che per-squadra. Un'espressione di budget come `remaining_G1 + 100` (G2) usava quindi il residuo dell'ULTIMA squadra processata per quel round, non quello della squadra a cui il budget apparteneva — riproduco con test: squadra A spende 190/200 in G1 (residuo 10), squadra B spende 50/200 (residuo 150); il budget G2 di A veniva calcolato come 250 (10+100 sostituito da 150+100) invece di 110. Bug mai emerso prima perché `generate_demo_events()` (fixture M0) processa un round per intero prima del successivo, e nessun test esistente verificava il VALORE numerico del budget G2 (solo `spent <= available`, che un budget erroneamente più alto non viola). Avrebbe corrotto silenziosamente i budget reali di ogni squadra tranne l'ultima processata per round, nell'asta vera. Corretto: `remaining_by_round_per_team: dict[str, dict[str, int]]`, keyed per team_id prima che per round_id. Trovato anche un bug di threading minore (SQLite + `st.cache_resource` di Streamlit su thread diversi): corretto con `check_same_thread=False`, sicuro per un'app locale single-user senza scrittori concorrenti.
- Rationale: entrambi i bug sono stati scoperti testando onestamente il flusso reale in browser (non solo unit test), esattamente la disciplina richiesta da CLAUDE.md per le modifiche UI. Il bug di budget cross-team è correttezza critica per l'asta reale (M0's gate "budget conservato" era rispettato solo nel senso ristretto spent<=available, non nel senso che il numero fosse quello giusto) — corretto immediatamente invece di rimandato, coerente con "make the smallest coherent change" applicato alla causa radice, non a un workaround nella UI.
- Conseguenze: verificato end-to-end in browser reale (non solo unit test): registrazione G1 (Dimarco, team_01, 190 crediti) → G2 (Leao, team_01, 35 crediti, budget calcolato correttamente 110 = 10+100) → undo del G2 (Leao liberato, budget G2 tornato a "nessuno") → tetto di riferimento nella scheda Leao coerente con la formula (budget 110, slot 23, quota VAR 5,3%, massimo 5). 258 test passano (13 nuovi: 9 `ledger_store.py`, 3 `effective_events`/carryover in `test_domain_replay.py`, 1 threading implicito nei test esistenti). Dati di test locali rimossi da `data/local/ledger.sqlite3` prima di chiudere (non dati reali d'asta).
- Approvato da: project owner

### ADR-2026-029 — M4 slice 3: spiegabilità permanente dei numeri derivati (VAR, fantavoto, massimo consigliato)

- Data: 2026-08-11
- Stato: approved
- Scope: ux
- Decisione: su richiesta esplicita dell'utente ("vorrei che sia tutto ben spiegato... da dove viene il VAR di Lautaro Martinez?"), `docs/UX_PRODUCT.md` registra un principio **permanente**, non solo di questa slice: ogni numero derivato mostrato in UI deve avere un tooltip breve al passaggio del mouse e, dove sensato, un pannello espandibile con la traccia di calcolo reale — i valori veri del record selezionato, non testo statico generico. Implementato in `app/pages/1_Giocatori.py` (tooltip `help=` su tutte le metriche VAR/fantavoto/massimo consigliato/quotazione; due pannelli "Come si calcola questo numero?" con aritmetica reale per VAR+fantavoto atteso e per il massimo consigliato) e `app/pages/2_Squadre.py` (tooltip sulle colonne del tabellone via `column_config`). Nessun nuovo calcolo nella UI: solo scomposizione di numeri già prodotti dal motore (`replacement_level`, `player_games_in_pool`, i campi di `MaxBidRecommendation`), mai un LLM che genera la spiegazione.
- Rationale: verificato con l'esempio esatto dell'utente — la scheda di Martinez L. (Lautaro) mostra: fantavoto atteso 7,64 (167 partite proprie nello storico, peso storia propria 167/(167+60)=73,6%), VAR = 7,64 − 6,09 (livello di replacement, 100° attaccante in lega su 100 slot totali) = 1,55. Verificata anche la traccia del massimo consigliato con dati reali dal ledger (budget 110, riserva 22 slot, discrezionale 88, quota VAR 5,3%, massimo 5).
- **Due bug reali trovati e corretti durante la verifica in browser**: (1) `app/pages/1_Giocatori.py` catturava solo `BidRecommendationError` attorno a `recommend_max_bid`, non `ConfigError` — con un ledger senza eventi nel round precedente necessario (es. G2 prima di G1), la pagina andava in crash non gestito invece di mostrare un messaggio chiaro; corretto aggiungendo `except ConfigError`. (2) Stesso gap in `app/pages/2_Squadre.py`'s form di registrazione (`except DomainError` non copriva `ConfigError`); corretto con `except (DomainError, ConfigError)`.
- Conseguenze: nessun nuovo test automatico (slice puramente di visualizzazione, nessuna nuova logica di dominio) — 258 test esistenti continuano a passare; verificato in browser con dati reali per entrambi i bug fix e per entrambe le tracce di calcolo. Principio di spiegabilità documentato in `docs/UX_PRODUCT.md`, vincolante per le slice M4 future.
- Approvato da: project owner

### ADR-2026-030 — M4 slice 4: rettifica di budget generica nel ledger; regola bonus logo personalizzato (+3 crediti)

- Data: 2026-08-11
- Stato: approved
- Scope: domain | architecture | ux
- Decisione: nuovo campo versionato `league.custom_logo_bonus_credits: 3` in `config/auction_rules.v1.yaml` (mai un letterale nel codice), con provenienza esplicita in commento ("Postilla admin, 2026-08-11: come lo scorso anno..."). `src/fantacalcio/domain.py` aggiunge `BudgetAdjustmentEvent` (event_id, ts, round_id, team_id, amount, reason, author) — meccanismo **generico** per qualsiasi rettifica di budget con causale (non solo il bonus logo; `amount` può essere negativo per un'eventuale penalità futura, nessuna nota ora), gestito in `replay()` sommando `amount` al budget disponibile della squadra per quel round (creandolo se non esiste ancora, stessa logica di `AssignmentEvent`). Applicato di norma a G1: si propaga automaticamente a G2/G3/G4 tramite la catena `remaining_G1` già esistente, senza logica speciale altrove. `effective_events()` esteso per includerlo (voidabile come gli altri tipi). Serializzazione in `src/fantacalcio/ledger_io.py` (tipo `"budget_adjustment"`), persistenza in `src/fantacalcio/persistence/ledger_store.py` (nessuna modifica necessaria, già generica). UI in `app/pages/2_Squadre.py`: form "Assegna bonus" (solo squadre non ancora premiate), colonna "Bonus logo" nel tabellone con tooltip, undo esteso a questo tipo di evento.
- Rationale: la regola è reale e comunicata dall'utente direttamente (non dedotta), analoga a un anno precedente — non richiede un ADR di "non ancora confermato" come altre regole in `docs/OPEN_QUESTIONS.md`. Il meccanismo scelto (evento di ledger generico, non un flag ad-hoc) rispetta CLAUDE.md ("preserve an append-only, replayable ledger for auction events"): il bonus è un fatto amministrativo con la stessa natura di un'assegnazione, quindi vive nello stesso ledger append-only, con lo stesso undo, senza introdurre un secondo canale di stato.
- Conseguenze: verificato end-to-end in browser con dati reali: bonus assegnato a `team_01` (G1 disponibile 200→203), poi un acquisto reale da 203 crediti registrato senza overspend (conferma che il tetto usato è quello corretto, non quello base). 269 test passano (11 nuovi: 1 config, 7 `domain.py`/`effective_events`, 3 `ledger_io.py`). Dati di test locali rimossi da `data/local/ledger.sqlite3` prima di chiudere.
- Approvato da: project owner

### ADR-2026-031 — M4 slice 5: prima fetta del costruttore rosa — vista "La mia rosa" + lock con verifica di fattibilità

- Data: 2026-08-11
- Stato: approved
- Scope: domain | ux
- Decisione: `src/fantacalcio/persistence/locks_store.py` (tabella SQLite `locks`, stesso DB del ledger ma tabella separata — i lock sono intenzione pre-asta, mai eventi di dominio, non entrano mai in `domain.replay()`). `src/fantacalcio/auction/lock_feasibility.py`: verifica pura (nessun I/O) prima di persistere un lock — rifiuta un giocatore già nella rosa reale della squadra (superfluo), già assegnato a un'altra squadra nel ledger reale (impossibile, spiegato con l'`team_id` reale), già bloccato, o che farebbe superare la capacità di ruolo (rosa reale + lock esistenti), spiegando sempre quale vincolo e cosa rimuovere per fare spazio — CLAUDE.md: "Locked players remain locked. If infeasible, explain the conflicting constraint and minimum relaxation." Nuova pagina `app/pages/3_Rosa.py`: rosa reale (da replay del ledger) per ruolo con slot pieni/vuoti e budget per round, sezione lock separata e sempre etichettata come ipotetica con costo stimato (somma quotazioni, non un'offerta), lock/unlock. Pulsante blocca/sblocca aggiunto anche alla scheda giocatore in `app/pages/1_Giocatori.py`.
- Rationale: il formato file admin resta sconosciuto (arriva venerdì sera, `docs/OPEN_QUESTIONS.md`), quindi l'import automatico resta bloccato; il costruttore rosa completo (drag-and-drop, ottimizzatore "rosa ideale"/"undici ideale", confronto moduli) richiede un motore di ottimizzazione non ancora costruito ed è troppo grande per una singola unità — questa slice ne costruisce il primo strato verificabile senza inventare un ottimizzatore: solo vista + lock con verifica di fattibilità onesta, mai un lock infeasible applicato silenziosamente.
- Conseguenze: verificato end-to-end in browser: Lautaro Martinez bloccato come obiettivo di `team_01` dalla scheda giocatore, comparso correttamente nella pagina Rosa (costo stimato 35 crediti, dalla quotazione reale), sbloccato con successo. 284 test passano (23 nuovi: 7 `locks_store.py`, 8 `lock_feasibility.py`, più le integrazioni). Nessun dato di test residuo nel ledger/locks locali.
- Approvato da: project owner

### ADR-2026-032 — Colma il gap: registrazione blocco portieri in Squadre (bug bloccante trovato in autonomia)

- Data: 2026-08-11
- Stato: approved
- Scope: ux
- Decisione: `app/pages/2_Squadre.py` aggiunge un modulo dedicato "Registra blocco portieri" (3 campi nome, una squadra, un prezzo unico per il blocco) che costruisce un `AssignmentEvent` con `role=GK`, `pool_id="goalkeeper_blocks"`, `item.player_ids` con i 3 codici giocatore risolti per nome (filtrati per ruolo `P`). Se i tre portieri non sono dello stesso club (`ruleset.roster.goalkeeper_same_club`), un avviso esplicito lo segnala — non bloccante, perché il vincolo "stesso club" non è ancora imposto a livello di dominio (`domain.py`'s `gk_block_identity` resta un placeholder, M1), quindi la UI non può far finta di applicarlo con certezza, solo avvisare onestamente.
- Rationale: trovato durante un'esplorazione autonoma di cosa restava da fare senza dati admin ("fai tutto quello che puoi fare senza input admin"). Il modulo di registrazione esistente (ADR-2026-028) rifiutava esplicitamente ogni tentativo di registrare un portiere ("non ancora gestito da questo form") — ma ogni squadra DEVE comprare un blocco portieri in G1 (regola di rosa obbligatoria), quindi senza questo il ledger non poteva rappresentare un'asta G1 reale e completa per nessuna squadra. Bug bloccante per l'uso reale dello strumento, non solo una funzionalità mancante minore — corretto immediatamente.
- Conseguenze: verificato end-to-end in browser con portieri reali (Svilar/Meret/Di Gregorio, club diversi): evento registrato correttamente (3 player_ids, un prezzo, budget G1 aggiornato), confermato via query diretta del ledger. 284 test esistenti continuano a passare (nessuna nuova logica di dominio, solo UI — replay() già gestiva correttamente i blocchi portieri dal M0). Dati di test locali rimossi prima di chiudere.
- Approvato da: project owner

### ADR-2026-033 — M4 slice 6: "rosa ideale" — ottimizzatore esatto di completamento rosa entro budget

- Data: 2026-08-11
- Stato: approved
- Scope: domain | ux
- Decisione: `src/fantacalcio/auction/roster_optimizer.py` risolve un knapsack 0/1 multi-vincolo (budget totale + capacità per ruolo) tramite programmazione dinamica esatta, non un'euristica greedy — massimizza il VAR totale della combinazione scelta tra i candidati non ancora presi (esclusi rosa reale e lock). Per trattabilità computazionale, il pool di candidati è limitato ai migliori `TOP_N_PER_ROLE=25` per VAR per ruolo prima di risolvere — approssimazione esplicita e dichiarata (mai un candidato scartato silenziosamente: il risultato riporta se il pool è stato limitato). L'obiettivo è "massimizza il VAR totale", non "riempi sempre ogni slot a ogni costo": uno slot resta volutamente vuoto se nessun candidato affidabile entro budget ha VAR sufficiente a giustificarlo — comportamento dichiarato esplicitamente nel modulo e testato. Integrato in `app/pages/3_Rosa.py`: nuova sezione "Rosa ideale" per round selezionato, budget = residuo reale dal ledger meno il costo stimato dei lock già bloccati per i ruoli di quel round, slot = target di ruolo meno rosa reale meno lock.
- Rationale: il formato file admin resta sconosciuto (venerdì sera), quindi resta la priorità più alta e fattibile del costruttore rosa (`docs/UX_PRODUCT.md`) costruibile senza quei dati. Scelta la programmazione dinamica esatta invece di un'euristica perché "rosa ideale" è esplicitamente uno strumento di pianificazione su cui l'utente farà affidamento per decisioni di credito reali — un'euristica greedy non garantita ottima sarebbe stata più semplice ma meno onesta rispetto al nome della funzionalità. Verificata la trattabilità computazionale con dati realistici (fino a 3 ruoli, ~180 candidati, budget~200): sotto 1,5 secondi.
- Conseguenze: verificato end-to-end in browser: calcolo rosa ideale per G1 (8 difensori, budget 200) ha prodotto una combinazione reale entro budget (costo 107, VAR totale 4.18) usando dati veri della tabella giocatori. 295 test passano (11 nuovi per `roster_optimizer.py`). Fuori scope dichiarato: "undici ideale" (richiede probabilità di voto/rischio SV, non ancora esposta nella tabella giocatori — dato mancante, non deciso ora), confronto tra moduli, profili prudente/bilanciato/aggressivo.
- Approvato da: project owner

### ADR-2026-034 — Probabilità di voto (rischio SV) collegata alla scheda giocatore

- Data: 2026-08-11
- Stato: approved
- Scope: data | ux
- Decisione: `scripts/run_m3_replacement_values.py` collega `src/fantacalcio/modeling/participation.py`'s `latest_known_participation()` (già costruito e validato, mai prima collegato all'output finale) alla tabella `_m3_replacement_values.csv`: nuove colonne `participation_rate`, `participation_season`, `participation_seasons_of_history`. Aggiunte a `REQUIRED_COLUMNS` in `src/fantacalcio/persistence/player_table.py`. `app/pages/1_Giocatori.py` mostra il tasso come "probabilità di voto (rischio SV), stima" con provenienza esplicita (stagione di riferimento, numero di stagioni di storico), o un messaggio onesto di non disponibilità per i giocatori a zero storico — mai un numero inventato. Rimossa dalla lista "non ancora disponibile in questa vista".
- Rationale: identificato come dato mancante ma già calcolabile durante l'esplorazione autonoma di cosa restava da fare senza input admin — il modello di partecipazione esisteva dal M2 ma non era mai stato collegato all'output finale usato dalla UI, un gap di collegamento (come Dixon-Coles→Monte Carlo nel blocco 1 della sequenza precedente), non un dato mancante per davvero.
- Conseguenze: 498 giocatori nella tabella, 84 senza tasso (zero storico, coerente con `data_quality_tier`). Verificato in browser con dati reali (Lautaro Martinez: 79%, stagione 2025/26, 5 stagioni di storico). 295 test esistenti continuano a passare (nessuna nuova logica di dominio, solo collegamento dati già validati). "Undici ideale" resta comunque bloccato: la probabilità di voto da sola non basta a un ottimizzatore di formazione titolare (servirebbe anche un modello di minutaggio/rischio per giornata specifica, non ancora costruito).
- Approvato da: project owner

### ADR-2026-035 — M4 slice 7: passata di chiarezza UI + rischi di rosa (concentrazione + da evitare)

- Data: 2026-08-11
- Stato: approved
- Scope: ux | domain
- Decisione: due parti, richieste insieme dall'utente dopo aver provato l'app in prima persona ("così non si capisce niente"). **(1) Chiarezza**: `src/fantacalcio/persistence/team_labels_store.py` (tabella SQLite `team_labels`, solo cosmetica — il ledger continua a usare `team_id` come unica chiave di dominio, mai un'etichetta come chiave, CLAUDE.md's entity-resolution rule) permette all'utente di dare un nome scelto da sé alla propria squadra, mostrato ovunque al posto dell'identificativo grezzo (`"I Bocconiani (team_01)"` invece di solo `team_01`). Ogni pagina (`app/Home.py`, `app/pages/*.py`) ora apre con una spiegazione in italiano semplice di cosa fa e come si usa — prima l'unico testo visibile in cima era il titolo. Valori tecnici (turno G1-G4, tier di qualità dati, ruolo) tradotti con le stesse mappe ovunque compaiono, non solo nella scheda giocatore. Home riscritta come guida reale (le tre aree, un flusso d'uso consigliato) invece che solo stato dei dati. **(2) Rischi di rosa**: `src/fantacalcio/persistence/avoid_list_store.py` (simmetrico a `locks_store.py`, tabella separata `avoid_list` — un giocatore da evitare non è mai anche un obiettivo, ambiguità strutturalmente impossibile) e `src/fantacalcio/auction/roster_risk.py` (conteggio puro, nessuna nuova statistica: quanti giocatori della rosa reale + lock vengono dallo stesso club reale, soglia di avviso dichiarata `DEFAULT_WARNING_THRESHOLD=3`). Integrati in `app/pages/1_Giocatori.py` (avviso + pulsante evita/rimuovi) e `app/pages/3_Rosa.py` (sezioni dedicate).
- Rationale: la chiarezza ha priorità sulle nuove funzionalità costruite sopra una UI che l'utente stesso ha definito incomprensibile — corretto prima di procedere oltre, anche se la richiesta di "rischi di rosa" era già stata concordata in precedenza. Verificando in browser con la sessione reale dell'utente (non un ledger di test, il loro), trovato un vero bug: colonne di tabella con tipo misto intero/stringa (`budget.remaining if budget else "—"`) causavano un errore di conversione Arrow silenziosamente recuperato da Streamlit ma con traceback nei log — corretto forzando `str()` su ogni colonna di questo tipo in `app/pages/2_Squadre.py` e `app/pages/3_Rosa.py`. Rimosso anche il rumore nei log da `use_container_width` (deprecato, sostituito con `width="stretch"`).
- Conseguenze: verificato in browser, incluso sulla sessione reale dell'utente (nome squadra "I Bocconiani" impostato e propagato correttamente su tutte le pagine; azione di test rimossa subito dopo la verifica per non alterare la loro pianificazione reale). 314 test totali passano (19 nuovi: 7 `team_labels_store.py`, 7 `avoid_list_store.py`, 5 `roster_risk.py`). Nessun bug residuo nei log dopo il fix.
- Approvato da: project owner

### ADR-2026-036 — Simulazione asta completa (4 turni, 20 squadre): tutti gli invarianti reggono; scoperta carenza reale di portieri

- Data: 2026-08-11
- Stato: approved
- Scope: domain | data
- Decisione: `scripts/run_auction_simulation.py` gioca un'asta completa e realistica (4 turni, 20 squadre, dati reali 2026/27) usando il codice reale del prodotto — non una simulazione separata: `domain.replay()`, `ledger_store`, `recommend_max_bid()`, `check_lock_feasibility()`, `optimize_roster_completion()` — su un database SQLite isolato e temporaneo (`data/local/_simulation_ledger.sqlite3`, cancellato a ogni esecuzione), **mai** il ledger reale dell'utente. Prezzo di ogni acquisto simulato calcolato dalla stessa funzione `recommend_max_bid()` usata dalla UI, non da una formula separata — la simulazione fa così anche da stress-test della formula su molti più scenari di quanti ne coprisse la demo precedente (`scripts/run_m3_bid_recommendation_demo.py`).
- **Scoperta reale**: solo **59 portieri** disponibili nel listone 2026/27 contro **60 slot richiesti** (3 per squadra × 20 squadre) — carenza di lega, anche minima, mai notata prima. Due club (Cagliari, Lecce) hanno solo 2 portieri reali nel listone: per una squadra che punta al loro blocco portieri, un blocco dello stesso club è **strutturalmente impossibile**, non solo sfortuna d'asta. La simulazione mostra l'effetto concreto: 19/20 squadre completano un blocco (una con club misti, nessun club con 3 liberi rimasti), l'ultima squadra non riesce a completarlo affatto (solo 2 portieri rimasti nel pool). Carenza analoga a quella già nota per gli attaccanti (ADR-2026-019: 88 disponibili contro 100 richiesti), confermata di nuovo esattamente dalla simulazione (40 in G2 + 48 in G3/G4 = 88).
- Rationale: su richiesta esplicita dell'utente di "arrivare a un prodotto finale da provare" e "simulare le fasi dell'asta" — verificare l'intero sistema con un'esecuzione realistica end-to-end, non solo con unit test isolati, è esattamente come sono stati trovati i bug precedenti più seri (ADR-2026-028 sul budget cross-team, ADR-2026-032 sul blocco portieri). Due bug trovati durante la costruzione dello script di simulazione stesso (non nel prodotto): un file SQLite temporaneo non veniva chiuso prima di essere cancellato (Windows blocca la cancellazione di file aperti) e un test di rifiuto-lock cercava per errore il primo giocatore già assegnato senza escludere la squadra che stava testando, finendo per testare il ramo sbagliato ("già nella tua rosa" invece di "assegnato a un'altra squadra") — entrambi corretti, nessuno dei due era un bug del prodotto.
- Conseguenze: **tutti gli invarianti verificati rispettati** su un'esecuzione realistica completa: budget mai negativo né sforato per nessuna delle 20 squadre in nessun turno, nessun ruolo mai oltre il tetto, nessun giocatore assegnato due volte, replay deterministico (due repliche dello stesso ledger danno risultati identici), rifiuto di lock su giocatori già assegnati con la squadra corretta nel motivo, ottimizzatore mai oltre budget. Report completo in `data/staged/fantacalcio_voti_manual/_auction_simulation_report.md` (locale). Script riutilizzabile prima dell'asta reale per una verifica finale; non incluso in `pytest -q` (33 secondi, quasi triplicherebbe il tempo della suite) — resta un diagnostico a esecuzione manuale, come gli altri script `scripts/run_*`.
- Approvato da: project owner

### ADR-2026-037 — Avvisi di mercato in Home: carenza per ruolo e club senza blocco portieri

- Data: 2026-08-11
- Stato: approved
- Scope: domain | ux
- Decisione: `src/fantacalcio/auction/market_supply.py` (conteggio puro, nessuna nuova statistica) confronta quanti giocatori esistono davvero nel listone 2026/27 per ruolo con quanti slot servirebbero in tutta la lega (letti da config, mai un letterale) — e quali club reali hanno meno portieri del necessario per un blocco dello stesso club. Nuova sezione "Avvisi di mercato" in `app/Home.py`, visibile subito, con le carenze reali trovate dalla simulazione (ADR-2026-036): portieri (-1), attaccanti (-12), e i due club (Cagliari, Lecce) senza abbastanza portieri per un blocco proprio.
- Rationale: dato reale e azionabile per la pianificazione pre-asta, scoperto durante l'esplorazione autonoma richiesta dall'utente ("integra anche nuovi dati") — non serviva un nuovo file esterno, solo collegare un conteggio già implicito nei dati già presenti (config + listone), esattamente come i blocchi precedenti (Dixon-Coles→Monte Carlo, partecipazione→scheda giocatore) erano gap di collegamento, non dati mancanti.
- **Bug reale trovato e corretto durante la verifica in browser**: la colonna "Carenza" della nuova tabella mischiava interi e il segnaposto `"—"` (stesso schema di tipo misto già corretto altrove in ADR-2026-035) — stesso errore di conversione Arrow nei log, stesso fix (`str()` esplicito). Verificato con un controllo su tutte le pagine (`grep 'else "—"'`) che non restassero altre istanze non corrette.
- Conseguenze: verificato in browser, dati reali confermano esattamente i numeri della simulazione. 320 test totali passano (6 nuovi per `market_supply.py`). Ledger reale dell'utente verificato intatto (1 evento, invariato) durante tutta questa sessione autonoma.
- Approvato da: project owner

### ADR-2026-038 — Audit adversariale (data-quality + statistico): tre gap reali corretti, nessun bug bloccante

- Data: 2026-08-12
- Stato: approved
- Scope: domain | ux
- Decisione: due subagent di audit dedicati (`data-quality-reviewer`, `statistical-reviewer` — CLAUDE.md's delegation policy, uso bounded/read-only) hanno esaminato l'intera pipeline (ingestion → entity resolution → modeling → persistenza → UI) e il nucleo statistico (target, leakage, calibrazione, Monte Carlo, inferenza previsione→offerta) in modo indipendente e avversariale, senza vedere il lavoro dell'altro. Data-quality: nessun blocco, un solo gap cosmetico (freschezza dati parziale in Home, vedi sotto). Statistico: nessun bug di leakage o join (walk-forward confermato corretto ovunque), due gap reali corretti in questa ADR, tre miglioramenti minori non bloccanti loggati ma non implementati (fallback discrezionale piatto per VAR molto negativo in `bid_recommendation.py:126`, clamp mancante sull'aggiustamento forza-squadra, FVM-prior e Dixon-Coles mai validati congiuntamente sul sottogruppo che li usa entrambi).
  1. **Freschezza dati incompleta (data-quality)**: `app/Home.py` mostrava solo `built_at` (quando la tabella DuckDB è stata (ri)costruita), non quando il calcolo sottostante (Monte Carlo + replacement values) è stato effettivamente generato — i due possono divergere se si ricostruisce la tabella da un CSV invariato. `src/fantacalcio/persistence/player_table.py`: nuovo campo `source_generated_at` (mtime del CSV sorgente) accanto a `built_at`, entrambi mostrati distintamente in Home.
  2. **P10-P90 dichiarato come certezza mai verificata (statistico, finding B1)**: il tooltip diceva "80% dei possibili esiti cade in questo range" come un fatto, ma nessun backtest di copertura empirica esisteva in tutto il repository (grep su "coverage"/"calibrat": zero risultati) — un rischio diretto di falsa precisione (CLAUDE.md: "show uncertainty... not false precision"). `scripts/run_monte_carlo_fantavoto.py`'s `part_a_validation` ora calcola la copertura empirica reale riusando lo stesso split walk-forward già validato per la media (ADR-2026-018): quale frazione della fantamedia reale 2025/26 cade nell'intervallo [P10,P90] simulato. Risultato reale misurato: **95.2%** su 562 giocatori (obiettivo nominale 80%) — la banda è più larga del necessario quando confrontata con un aggregato di stagione, non più stretta: nessuna falsa precisione trovata, ma prima d'ora non era mai stato verificato, solo assunto. Scritto in `data/staged/fantacalcio_voti_manual/_monte_carlo_validation_meta.json` (nuovo, letto da `scoring/monte_carlo.py`'s `load_calibration_meta()`); `app/pages/1_Giocatori.py` mostra ora il numero reale misurato invece dell'80% nominale, con fallback esplicito se il file non esiste ancora.
  3. **Livello di replacement degenere non segnalato per riga (statistico, finding B2)**: `replacement.py` già gestiva correttamente la carenza di offerta per ruolo (portieri, attaccanti — ADR-2026-036/037) a livello aggregato, ma quando succede il livello di replacement diventa silenziosamente "il peggiore disponibile" invece di "il miglior escluso", il che spinge meccanicamente il VAR di ogni giocatore di quel ruolo verso l'alto — un cambio reale nel significato del numero, non solo rumore in più, e la traccia "come si calcola" per-giocatore non lo segnalava mai. Nuovo campo booleano `degenerate_replacement` in `add_value_above_replacement()` (`replacement.py`), propagato in `REQUIRED_COLUMNS` (`player_table.py`) e mostrato come avviso esplicito nella traccia VAR di `app/pages/1_Giocatori.py` per ogni giocatore di un ruolo in carenza.
  - Corretto anche un difetto minore di formulazione (non bloccante, dal report statistico): la didascalia del limite del pool candidati nell'ottimizzatore (`app/pages/3_Rosa.py`) riportava il totale complessivo come se fosse il limite per-ruolo — ora usa `TOP_N_PER_ROLE` esplicitamente e mostra entrambi i numeri.
- Rationale: gli audit erano stati richiesti dalla direttiva "fai tutto quello che puoi fare, tutto tutto. usa agenti e subagenti come nella struttura del progetto" — la prima esecuzione con `isolation: "worktree"` è fallita in modo ambientale (non recuperabile via `SendMessage`, verosimilmente Windows + percorso OneDrive con spazi che interagisce male con i worktree git), rieseguita senza isolamento con successo. Ogni finding è stato verificato con citazione di file/riga prima di essere accettato, non applicato ciecamente; i tre miglioramenti non bloccanti sono stati deliberatamente non implementati (rischio/beneficio non abbastanza chiaro senza una decisione esplicita sulla robustezza ai casi limite, fuori scope per una correzione guidata da audit).
- Conseguenze: pipeline completa rieseguita (`run_monte_carlo_fantavoto.py` → `run_m3_replacement_values.py` → `build_player_table.py`) per generare i nuovi campi/metadati sui dati reali 2026/27; verificato in browser che entrambi i fix compaiano correttamente (avviso di carenza su un attaccante reale, tooltip P10-P90 con il numero misurato reale), nessun errore in console o nei log del server. 321 test totali passano (1 nuovo netto: test di `degenerate_replacement`, assert aggiuntivo su `source_generated_at`). Ledger reale dell'utente verificato intatto durante il riavvio del server necessario per sbloccare il file DuckDB.
- Approvato da: project owner

### ADR-2026-039 — Confronto moduli: forza rosa per modulo, non un nuovo target d'asta

- Data: 2026-08-12
- Stato: approved
- Scope: domain | ux
- Decisione: prima di implementare, verificato nei numeri reali (`config/auction_rules.v1.yaml`) che la rosa fissa (3 portieri, 8 difensori, 8 centrocampisti, 5 attaccanti) copre già contemporaneamente il fabbisogno di titolari di **tutti** gli 8 moduli configurati (il più esoso per ruolo, 5D/5C/3A a seconda del modulo, è sempre sotto il conteggio fisso posseduto) — quindi estendere `roster_optimizer.py` con un target di slot diverso per modulo non avrebbe cambiato alcun risultato: i target sarebbero sempre stati dominati dai conteggi fissi già esistenti. Segnalato esplicitamente all'utente prima di procedere (CLAUDE.md: "never silently resolve a conflict"), che ha scelto lo scope corretto una volta chiarito.
  Implementato invece `src/fantacalcio/auction/formation_strength.py` (conteggio puro, nessuna nuova formula sui numeri del motore): per ciascuno degli 8 moduli, sceglie i migliori giocatori posseduti (rosa reale + obiettivi bloccati) per ruolo fino al fabbisogno di quel modulo e ne somma il VAR — mostra con quale modulo la rosa **reale** dell'utente rende di più, in **media di stagione**. Sezione "Confronto moduli" in `app/pages/3_Rosa.py`, tra "Concentrazione di squadra" e "Rosa ideale".
- Rationale: dichiarato esplicitamente e ripetutamente nella UI che questa è un'approssimazione onesta di "undici ideale" (docs/UX_PRODUCT.md), non un sostituto: usa solo il VAR medio di stagione, non dati di rischio per giornata (probabile formazione, infortuni, avversario) che non esistono ancora nella pipeline (docs/OPEN_QUESTIONS.md) — "undici ideale" vero resta bloccato in attesa di una fonte dati esplicita per quello. Un modulo non completamente copribile con la rosa attuale non viene mai nascosto: `fully_coverable=False` e `missing_by_role` sempre esposti, mai un numero silenziosamente ottimistico.
- Conseguenze: 328 test totali passano (7 nuovi per `formation_strength.py`). Verificato in browser: la sezione mostra correttamente lo stato "non ancora copribile" con la rosa reale attuale dell'utente (solo obiettivi bloccati, nessun acquisto reale ancora), nessun errore in console/log. Nessuna colonna a tipo misto (pattern di bug già visto tre volte in questa sessione, controllato esplicitamente questa volta prima di considerare fatto).
- Approvato da: project owner

### ADR-2026-040 — Rifiutata l'idea di un LLM che cerca dati in rete; niente scraping via intermediario

- Data: 2026-08-12
- Stato: approved
- Scope: process | domain
- Decisione: l'utente ha chiesto se un LLM economico non-Anthropic potesse "cercare informazioni su internet" (probabili formazioni, infortuni) per aggirare il divieto di scraping. Rifiutato esplicitamente: un LLM che recupera contenuto da un sito che vieta l'accesso automatizzato nei suoi ToS sta facendo esattamente lo scraping vietato da CLAUDE.md, indipendentemente dal modello usato come intermediario — non è una scappatoia, è la stessa violazione con un passaggio in più. In aggiunta, un LLM con web search può "allucinare" con più facilità proprio su notizie locali molto recenti (probabili formazioni cambiano ora per ora), e un output senza provenienza/`as_of` verificabile non passerebbe comunque il requisito di CLAUDE.md su incertezza/provenienza — un dato di input sbagliato spacciato per pulito è più pericoloso di un calcolo LLM esplicitamente segnalato come tale.
- Rationale: mantenere la distinzione netta già presente in CLAUDE.md ("un LLM può spiegare risultati ma non calcolarli") anche per l'ingestion dei dati grezzi, non solo per il calcolo — una fonte di input scelta autonomamente da un LLM è altrettanto pericolosa di un calcolo LLM, solo mascherata meglio.
- Conseguenze: nessuna modifica al codice. Proposta alternativa legittima e verificata al suo posto: API-Football (già parzialmente adottato, ADR-2026-010) a pagamento ($19/mese piano Pro, verificato 2026-08-12 via ricerca web sui prezzi ufficiali) per le probabili formazioni Serie A stagione corrente; per notizie/infortuni, import manuale di testo che l'utente stesso incolla da una fonte a cui è già abbonato, con l'LLM che fa solo parsing in struttura, mai ricerca autonoma.
- Approvato da: project owner

### ADR-2026-041 — Audit storico estero (API-Football): scoperta un vincolo reale, nessuna integrazione fatta

- Data: 2026-08-12
- Stato: approved
- Scope: data
- Decisione: quantificato il problema reale — 84 giocatori (`no_history_transfer`/`no_history_new_team` in `_m3_replacement_values.csv`) hanno zero storico perché arrivano da campionati non coperti dalla pipeline (solo Serie A oggi). Prima di proporre un'integrazione, verificato Understat (bloccato: `robots.txt` vieta ogni crawler, corretto il suo stato in `docs/SOURCE_REGISTER.md` da "ammesso con cautela" a "escluso") e testato API-Football con una chiamata reale (`scripts/run_foreign_history_audit.py`, nuova funzione `search_player()` in `src/fantacalcio/ingest/api_football.py`). **Scoperta reale**: l'endpoint `players` rifiuta una ricerca globale per nome — richiede già un `team`/`league` noto, e il nostro listone non ha un campo "club di provenienza" per risolverlo automaticamente. "Cercare ovunque" non è quindi possibile come integrazione in blocco; richiederebbe un hint per-giocatore fornito dall'utente (club precedente, da una fonte che l'utente già conosce/si fida), poi una chiamata mirata — `search_player()` supporta già questo caso via il parametro `team_id`, aggiunto proprio per questo. Nessuna chiamata sprecata: fermato il campione dopo la prima chiamata reale, dato che l'errore è un vincolo API deterministico, non specifico del giocatore cercato.
- Rationale: CLAUDE.md vieta join per solo nome — anche solo "cercare un giocatore per nome" su una fonte esterna senza uno scope già noto (club/lega) rischia di restituire l'omonimo sbagliato; il vincolo API stesso, per una ragione diversa (limite del piano), impone comunque la stessa disciplina che avremmo dovuto imporci noi.
- Conseguenze: 330 test totali passano (2 nuovi per `search_player`). Nessun dato scritto nella pipeline di dominio — puro discovery, come richiesto esplicitamente dall'utente ("audit gratuito prima"). Report completo in `data/staged/fantacalcio_voti_manual/_foreign_history_audit.md` (locale, gitignored). Prossimo passo dipende dall'utente: se vuole procedere, serve fornire i club di provenienza reali per i giocatori a quotazione più alta (Ramos G. 27, Mastantuono 12) — non inventabili da qui.
- Approvato da: project owner

### ADR-2026-042 — Hint di provenienza via subagent Haiku + web search: 2/5 giocatori con storico reale trovato

- Data: 2026-08-12
- Stato: approved
- Scope: data | process
- Decisione: su richiesta esplicita dell'utente ("lancia degli agenti Haiku per cercare"), lanciati 5 subagent Haiku indipendenti (uno per giocatore, `general-purpose`, `model: haiku`) con istruzione esplicita di usare solo ricerca web pubblica (notizie di mercato, comunicati ufficiali) e **non tentare di scrapare siti che bloccano l'accesso automatizzato** — stesso principio già applicato a mano per Understat (ADR-2026-041). Ogni agente ha restituito nome completo, club/lega di provenienza, livello di confidenza e fonti citate (comunicati ufficiali club, ESPN, beIN Sports, ecc.) — tutti con confidenza "alta" e fonti multiple concordanti. Con questi hint, risolto il vincolo scoperto in ADR-2026-041 (l'endpoint `players` di API-Football richiede un `team`/`league` noto): aggiunta `search_team()` (simmetrica a `search_player()`) per risolvere il `team_id` dal nome del club, poi interrogato `search_player(..., team_id=...)` per ciascuno dei 5 giocatori sulla stagione 2023 (unica disponibile sul piano gratuito).
  **Risultato reale**: 2 giocatori su 5 trovati con statistiche vere — Gonçalo Ramos (PSG, 2022/23: 11 gol, 1 assist, 29 presenze) e John Stones (Manchester City, 2022/23: 1 gol, 27 presenze) — invece del placeholder piatto "role-average" che hanno oggi in `data_quality_tier=no_history_transfer`. Gli altri 3 (Mastantuono, Kevin Carlos, Alajbegovic) non trovati nella stagione 2023 al club indicato dall'agente: i loro trasferimenti/prestiti più rilevanti sono più recenti (2024-2026) della finestra gratuita disponibile (2022-2024) o coinvolgono un club diverso da quello formalmente "di provenienza" (es. Alajbegovic: venduto e riacquistato dal Leverkusen, minutaggio vero al prestito Salisburgo 2025/26).
  **Bug reale trovato e corretto**: `_call()` in `src/fantacalcio/ingest/api_football.py` costruiva la query string concatenando i parametri senza URL-encoding — un nome con spazio ("Paris Saint Germain") produceva un URL non valido (`InvalidURL: control characters`). Sarebbe scattato anche su `search_player` con qualunque nome multi-parola (es. "Kevin Carlos"), bug preesistente mai emerso perché finora l'unico caller reale (`fetch_fixtures`) passava solo ID numerici. Corretto con `urllib.parse.urlencode`, test di regressione aggiunto.
- Rationale: la ricerca web generica per notizie di mercato pubbliche (riportate da decine di fonti indipendenti) è categoricamente diversa dallo scraping di un sito specifico che lo vieta nei suoi ToS/robots.txt (Fantacalcio.it, Understat) — è esattamente cosa farebbe l'utente stesso cercando su un motore di ricerca, solo delegato ad agenti economici per bounded lookup, coerente con la policy di delegazione di CLAUDE.md. L'output resta comunque puro discovery, mai scritto automaticamente nella pipeline di dominio: ogni hint andrebbe riverificato da un umano prima di un uso reale, dichiarato esplicitamente nel codice e nel report.
- Conseguenze: 332 test totali passano (2 nuovi: `search_team`, regression sull'URL-encoding). Nessuna scrittura nella pipeline di dominio. Budget API-Football usato: 10/100 richieste giornaliere. Report aggiornato in `data/staged/fantacalcio_voti_manual/_foreign_history_audit.md`. Prossimo passo dipende dall'utente: se vuole procedere a una vera integrazione (join per `player_code` con verifica manuale, mai per solo nome), serve decidere caso per caso quale stagione/club usare per i 3 giocatori non ancora trovati, e se vale la pena del piano a pagamento per coprire le stagioni più recenti dove probabilmente si trovano.
- Approvato da: project owner

### ADR-2026-043 — Copertura completa dei rimanenti 82 giocatori: 14 batch Haiku, tentata delega a Gemini (quota esaurita), 5 nuovi storici reali trovati

- Data: 2026-08-12
- Stato: approved
- Scope: data | process
- Decisione: su richiesta esplicita dell'utente ("continua sugli altri 82 giocatori, priorità quotazione alta, usa tantissimi agenti haiku e gemini"), lanciati **14 subagent Haiku in background** (raggruppati 6 giocatori per agente per restare gestibile, non 82 singoli — stesso principio "bounded" della delega Haiku in CLAUDE.md), ciascuno con le stesse istruzioni di sola ricerca web pubblica già validate in ADR-2026-042. Copertura totale: tutti gli 82 giocatori rimanenti, hint di club/lega raccolti con fonti citate.
  **Tentata anche la delega a Gemini** come richiesto, per un compito appropriato al suo ruolo nel progetto (non la ricerca di identità, esplicitamente vietata a Gemini da CLAUDE.md — spiegato all'utente prima di procedere): scrivere uno script di consolidamento del report in CSV, tramite `scripts/delegate_gemini.py`. **Fallito per un vincolo esterno reale**: quota giornaliera gratuita di Gemini (`gemini-3.5-flash`, 20 richieste/giorno) già esaurita al momento del tentativo — `TerminalQuotaError` diretto dall'API, non un bug del wrapper (verificato riproducendo la chiamata Python passo-passo). Nessun ulteriore tentativo fatto oggi.
  Con gli 82 hint raccolti, rieseguito `search_team`+`search_player` (script aggiornato con un dizionario `HINTS` completo, ordinato per quotazione decrescente) rispettando il budget giornaliero reale residuo (90 richieste, già scalate le 10 usate nell'audit precedente). **Risultato**: 5 ulteriori giocatori con statistiche reali trovate — Kaiki (Cruzeiro, Brasile), Mangas (Sporting CP), Viery (Grêmio, Brasile), Correia T. (Valencia), Milla (Getafe) — portando il totale a **7 su 84** con storico vero invece del placeholder piatto. Budget esaurito a 90/90 prima di coprire tutti gli 82 (47 giocatori unici testati, alcuni team_id condivisi/cachati es. Frosinone hanno risparmiato chiamate). Circa 35 giocatori a quotazione più bassa restano non testati oggi.
  **Scoperta aggiuntiva importante dagli agenti stessi**: diversi giocatori del tier `no_history_new_team` **non sono trasferimenti recenti** — sono giocatori già al club da anni (2023-2025), segnalati come "senza storico" solo perché il *club* (Frosinone, Como, Monza, Venezia) è neopromosso in Serie A quest'anno, non perché il giocatore sia nuovo. Marcato esplicitamente "DATA ISSUE" per ciascun caso nel dizionario hint del codice — utile per capire la vera semantica del tier, non solo per questa ricerca.
  **Bug/vincolo API in più trovato**: `players` search rifiuta nomi di ricerca sotto 4 caratteri (`Yan`, `Jay` — 3 caratteri) con un errore esplicito, non silenzioso — nessun fix di codice necessario (il messaggio d'errore è già chiaro), solo documentato per un futuro rerun.
- Rationale: batch da 6 invece di 82 agenti singoli per restare entro un uso ragionevole di risorse mantenendo comunque "molti agenti" come richiesto; rispettato il vincolo esplicito di CLAUDE.md su Gemini ed entity policy spiegandolo prima di procedere, non ignorandolo; fermata la richiesta API al budget reale residuo invece di sforare la quota gratuita giornaliera reale.
- Conseguenze: nessuna scrittura nella pipeline di dominio, puro discovery come tutte le fasi precedenti di questo filone. Report completo aggiornato in `data/staged/fantacalcio_voti_manual/_foreign_history_audit.md` (locale, gitignored) con tutti gli 82 hint e i risultati delle verifiche. Nessun test nuovo necessario (nessuna nuova funzione di libreria, solo dati/script). Prossimo passo: decisione dell'utente su se e quando riprendere i ~35 giocatori rimanenti (quota giornaliera si resetta, oppure piano a pagamento) e se procedere a una vera integrazione nella pipeline per i 7 giocatori con dati reali trovati finora (richiederebbe verifica manuale dell'identità prima di qualunque join per `player_code`, mai per nome).
- Approvato da: project owner

### ADR-2026-044 — Import lista admin ufficiale (Markdown) come oggetto `official` separato, con risoluzione identità esplicita

- Data: 2026-08-15
- Stato: approved
- Supersedes: chiude l'azione lasciata aperta in ADR-2026-002/CURRENT_TASK.md ("import del formato admin quando arriva")
- Scope: data | auction
- Decisione: la lista admin ufficiale 2026/27 (`Liste Fantacalcio 26_27.md`, ricevuta dall'utente) è un formato Markdown nuovo (9 blocchi "Lista N", rank + punteggio, solo nomi visualizzati, nessun `Id` stabile) — diverso dagli export xlsx già gestiti da `fantacalcio_listone.py`. Aggiunto un parser dedicato, `src/fantacalcio/ingest/admin_list_markdown.py`, che stage il file grezzo così com'è (immutabile, hash SHA-256, nessuna interpretazione) in `data/staged/admin_list_markdown/`. La **Lista 1 ("Portieri")** contiene in realtà nomi di squadra (Roma, Inter, Milan, ...), non singoli portieri — confermato dall'utente: è una quotazione per-squadra dello slot portiere, non un ranking per-giocatore. Quelle righe sono taggate `entity_type="team"` nel parser e vanno risolte contro l'identità squadra (`fantacalcio.identity.teams`), mai contro `player_code`. Per le liste 2-9 (giocatori veri), aggiunto `src/fantacalcio/identity/player_name_resolver.py`, stesso pattern già validato in `teams.py`: match esatto normalizzato o fuzzy ≥0.90 con vincolo di ruolo uguale, tutto il resto (ambiguo, sotto soglia, o omonimi non distinguibili) va in coda di revisione manuale, mai forzato.
- Rationale: CLAUDE.md vieta di unire giocatori solo per nome e impone stati `unknown`/`provisional`/`official` separati dal ranking modello; questa lista è l'oggetto `official` (fonte: file admin) per la prossima asta, non deve mai sovrascrivere silenziosamente il ranking del modello né essere fusa senza verifica.
- Conseguenze: sul file reale, 180 righe totali parsate (20 team-level in Lista 1 + 160 giocatori in Liste 2-9), **151/160 giocatori (94%) risolti automaticamente**, 9 in coda di revisione manuale (nessun match forzato) — per lo più giocatori assenti dal listone xlsx 2026/27 (es. trasferimenti/quotazioni non ancora presenti in quel file) o soglia fuzzy non raggiunta. 14 nuovi test (`tests/test_ingest_admin_list_markdown.py`, `tests/test_identity_player_name_resolver.py`), tutti passano. Nessuna scrittura automatica sul ledger/lock/roster: l'integrazione come oggetto `official` a valle della risoluzione, la verifica di coerenza con i 4 lock attivi e la revisione dei 9 casi in coda restano prossimo passo esplicito con l'utente.
- Approvato da: project owner

### ADR-2026-045 — Integrazione curata dell'admin list: 9 nuovi giocatori confermati, blocchi portiere per squadra risolti come oggetto separato

- Data: 2026-08-15
- Stato: approved
- Supersedes: chiude i tre punti aperti lasciati da ADR-2026-044
- Scope: data | auction
- Decisione: (1) l'utente ha confermato che tutti i 9 giocatori in coda di revisione (Chalobah, Isaksen, Jimenez, Molina N., Obrador, Rodriguez Je., Schmid, Spence, Sucic) sono giocatori reali assenti dal listone xlsx 2026/27, non omonimi/errori — tenuti con `player_code=None` (mai un ID inventato) e `identity_status="new_player_pending_code"`, non forzati su nessun candidato del match fuzzy. (2) La Lista 1 (blocchi portiere per squadra, vedi ADR-2026-044 e `goalkeeper_block` in `config/auction_rules.v1.yaml`, pool `goalkeeper_blocks` in G1 per `docs/AUCTION_RULES.md`) è risolta contro l'identità squadra vera (stessi 20 nomi squadra della Quotazioni 2026/27, match esatto su tutti e 20), tenuta come oggetto **separato** dai giocatori — mai unita alla tabella giocatori. (3) Costruito `src/fantacalcio/identity/admin_official_list.py`: prende lo staged frame del parser Markdown + la risoluzione nome→`player_code`, produce cinque frame distinti e onesti (`resolved_players` con `list_state="official"`, `new_players` senza `player_code`, `review_queue` residuo se mai non vuoto, `goalkeeper_blocks` per club, `unmatched_teams` se un nome club non matcha), scritti come CSV curati in `data/curated/admin_list_2026_27/` con hash sorgente e `as_of` in `_meta.csv` (livello `curated` per `docs/DATA_AND_MODELING.md`). Nessuna scrittura sulla tabella DuckDB `players` (ranking del modello): resta un oggetto separato, come richiesto da CLAUDE.md.
- Rationale: un ID player_code inventato per i 9 nuovi giocatori romperebbe la garanzia "mai join per solo nome" alla prima integrazione futura col vero listone (quando l'admin lo aggiornerà con quei nomi e un Id reale); tenerli espliciti e senza ID evita quel rischio. I blocchi portiere per squadra sono semanticamente un oggetto diverso dai giocatori (non hanno un ruolo individuale né uno `Id` giocatore) — unirli alla tabella `players` avrebbe richiesto un `player_code` fittizio, stesso problema.
- Conseguenze: sul file reale, 151 giocatori risolti (`list_state=official`), 9 nuovi giocatori confermati senza codice, coda di revisione **vuota** (0 residui), 20/20 blocchi portiere risolti, 0 squadre non matchate. 7 nuovi test (`tests/test_identity_admin_official_list.py`); durante la stesura, trovato e corretto un bug reale nello script diagnostico e nel test stesso: il nome confermato dall'utente era stato registrato come "Chalobah D" (nome+ruolo concatenati senza separatore in un print di debug) invece di "Chalobah" — avrebbe silenziosamente lasciato 3 giocatori reali in coda di revisione nonostante la conferma esplicita dell'utente. 352 test totali passano. Prossimo passo: decidere lo storage/consultazione di questi CSV curati dentro l'app (nuova pagina o sezione, non ancora fatto) e se/quando assegnare un `player_code` reale ai 9 nuovi giocatori (richiede il prossimo aggiornamento del listone admin, non un'azione di questa sessione).
- Approvato da: project owner

### ADR-2026-046 — Overlay `official` sulla tabella giocatori dell'app, cross-check lock, simulazione G1-G4

- Data: 2026-08-15
- Stato: approved
- Supersedes: chiude l'ultimo punto aperto di ADR-2026-045 (consultazione dei CSV curati dentro l'app) per la parte "quotazione ufficiale sul giocatore"; `round_pools.py` resta come modulo di calcolo del `round_pool`/`list_pool_name` — solo `list_state` viene corretto qui
- Scope: data | auction | UX
- Decisione: (1) **Cross-check dei 4 lock reali** (`data/local/ledger.sqlite3`, team_01: Thuram K./C, Diao/A, Malen/A, Carnesecchi/P) contro la lista curata — tutti e 4 coerenti, nessun conflitto: i tre giocatori di movimento sono presenti nella lista ufficiale col loro rank/punteggio admin, e Carnesecchi (portiere) è coerente perché l'Atalanta è presente tra i 20 blocchi portiere ufficiali (rank 7, punteggio 28) anche se lui individualmente non compare in Lista 1 (che è per club, non per giocatore — atteso). (2) Costruito `src/fantacalcio/auction/apply_official_admin_list.py`: overlay puro e deterministico che, dato il pool giocatori del modello + i frame curati (`resolved_players`, `goalkeeper_blocks`), imposta `list_state="official"` sui `player_code` risolti e sui portieri il cui club ha un blocco ufficiale, aggiungendo colonne `admin_rank`/`admin_score`/`admin_gk_block_score` senza mai toccare i numeri del modello. (3) `scripts/apply_admin_official_list.py` applica l'overlay a `_m3_replacement_values.csv` (idempotente, rieseguibile dopo ogni rigenerazione M3) e ricostruisce la tabella DuckDB (`src/fantacalcio/persistence/player_table.py`) che l'app legge. (4) Corretto un testo statico ora fuorviante: `app/Home.py` e `app/pages/1_Giocatori.py` dicevano incondizionatamente "lista provvisoria del modello, non ancora quella ufficiale dell'admin" — ora riflettono lo stato reale per-giocatore (`list_state` letto dalla tabella, non un'affermazione fissa). (5) Eseguita `scripts/run_auction_simulation.py` (4 turni, 20 squadre, dati reali) per stress-test end-to-end: tutti gli invarianti rispettati (budget mai negativo/sforato, nessun ruolo oltre tetto, nessun doppio acquisto, replay deterministico); confermate le carenze reali già note (59/60 portieri, 88/100 attaccanti; Cagliari e Lecce non riescono a formare un blocco portieri dello stesso club).
- Rationale: la lista admin ufficiale è ora disponibile (ADR-2026-044/045) ma l'app continuava a dichiarare tutto "provvisorio" — un'affermazione falsa che nasconderebbe proprio la distinzione `unknown`/`provisional`/`official` richiesta da CLAUDE.md. L'overlay è un merge puro (nessuna logica di scoring/modeling nuova, solo etichettatura di provenienza) per restare coerente con "tutto lo scoring/assignment è codice deterministico" e "mai sovrascrivere silenziosamente il ranking del modello".
- Conseguenze: `_m3_replacement_values.csv`/tabella `players`: 210/498 giocatori ora `list_state=official` (151 di movimento + 59 portieri via blocco club), 288 restano `provisional`. 6 nuovi test (`tests/test_auction_apply_official_admin_list.py`), 358 test totali passano. Verificato in browser: Home mostra "210 giocatori su 498 hanno la quotazione ufficiale dell'admin"; scheda giocatore mostra "lista `official` (ufficiale dell'admin)" per un giocatore risolto. Nessuna modifica al ledger reale (simulazione usa DB temporanei, cancellati a fine run). Prossimo passo eventuale: superficie dedicata per i 9 nuovi giocatori senza `player_code` e per i 20 blocchi portiere nell'app (oggi solo nei CSV curati, non ancora in una pagina/tabella dedicata).
- Approvato da: project owner

### ADR-2026-047 — Chiarezza UI per un utente completamente non tecnico: icone native, tema leggibile, niente dipendenze esterne

- Data: 2026-08-16
- Stato: approved
- Scope: UX
- Decisione: su richiesta esplicita dell'utente ("deve usare anche un mio amico che non capisce niente di computer", "vai completamente autonomo"), cercati pacchetti/repo per migliorare l'interfaccia (`streamlit-antd-components`, `streamlit-extras`, ecc. — vedi ricerca web). **Scelta di non aggiungere nessuna dipendenza esterna**: Streamlit 1.61 (già installato, `pyproject.toml`) offre nativamente tutto ciò che serve (icone pagina, tema, container con bordo, toolbar minimale) senza il rischio di manutenzione/supply-chain di un componente di terze parti su uno strumento "local-first" con dati privati. Applicato: (1) `.streamlit/config.toml` nuovo — `client.toolbarMode="minimal"` nasconde i pulsanti "Deploy"/GitHub/menu sviluppatore (irrilevanti in locale, rischio di click per errore per chi non ha mai usato Streamlit), tema con colori a contrasto più alto e raggio bordi più morbido. (2) Le 3 pagine rinominate con icona emoji nel nome file (convenzione nativa Streamlit per l'icona nella barra laterale): `1_🔍_Giocatori.py`, `2_📋_Squadre.py`, `3_⭐_Rosa.py`, più `page_icon` coerente in ogni `st.set_page_config` (icona anche nella scheda del browser). (3) `app/Home.py` ristrutturata: le tre pagine e i tre passi d'uso ora sono card visive con icone (`st.container(border=True)` + colonne) invece di elenchi puntati testuali; i dettagli tecnici (hash file, timestamp UTC) spostati in un pannello `st.expander` "Dettagli tecnici (non necessari per usare l'app)" invece di essere in prima vista.
- Rationale: per un utente che "non capisce niente di computer", il rischio maggiore non è la mancanza di funzionalità ma il rumore visivo — pulsanti sviluppatore senza senso, densità di testo/numeri tecnici in prima vista, pagine indistinguibili nella barra laterale. Una libreria di componenti esterna avrebbe aggiunto complessità/dipendenze per un beneficio marginale rispetto a usare bene le funzionalità già native dello strumento installato.
- Conseguenze: nessun nuovo test necessario (solo copy/layout, nessuna logica di dominio toccata) — 358 test esistenti continuano a passare invariati. Verificato in browser: pulsante "Deploy" scomparso, icone visibili in barra laterale (🔍/📋/⭐), route `/Giocatori` invariata (Streamlit rimuove l'emoji dallo slug URL), Home più scorrevole con dettagli tecnici collassati di default. File rinominati: nessun riferimento nel codice/test ai vecchi nomi file (solo negli ADR storici, lasciati intatti per non riscrivere la storia).
- Approvato da: project owner

### ADR-2026-048 — Repo pronto per deploy privato su Streamlit Community Cloud; ledger reale mai pubblicato

- Data: 2026-08-16
- Stato: approved
- Scope: operations | data
- Decisione: su richiesta dell'utente di usare lo strumento dal browser senza passare da `localhost` ogni volta, valutate tre opzioni (rete locale, tunnel temporaneo, Streamlit Community Cloud). **Verificato prima di procedere** che lo storage di Streamlit Community Cloud è dichiaratamente effimero (la piattaforma può cancellare i file locali, inclusi database SQLite, a ogni riavvio/redeploy — vedi discussioni ufficiali Streamlit citate in chat) — rischio reale per un ledger d'asta "append-only". Spiegato esplicitamente all'utente, che ha confermato: l'uso previsto non è la registrazione live dell'asta ("non è in tempo reale, mi serve per comodità"), quindi il rischio è accettabile per quell'uso. Preparato il repo per il deploy, senza creare account o pubblicare nulla in autonomia (creare account è un'azione vietata per l'assistente, da fare dall'utente): (1) `requirements.txt` nuovo (mirror di `pyproject.toml` + `-e .` per installare il pacchetto `fantacalcio` da `src/`, stesso meccanismo già in uso in locale). (2) `.gitignore` corretto: `data/local/` è passato da esclusione di cartella intera a `data/local/*` + eccezione mirata `!data/local/fantacalcio.duckdb` — necessario perché Git non permette di "riammettere" un file dentro una cartella già esclusa in blocco. Solo la tabella giocatori **calcolata** (fantavoto/VAR derivati) è ammessa; il ledger reale, i lock e le etichette squadra (`ledger.sqlite3`, stato live e privato dell'asta) restano **sempre** esclusi, anche da un eventuale repo privato — verificato con `git check-ignore`. L'utente ha scelto di procedere solo con un repo GitHub **privato**, mai pubblico.
- Rationale: CLAUDE.md impone "privato, local-first" come missione del progetto e vieta di esporre dati privati dei partecipanti — pubblicare il ledger reale (chi ha bloccato/vinto cosa, budget) violerebbe quella regola anche in un repo privato (più persone potrebbero vederlo, è comunque fuori dal computer dell'utente). La tabella giocatori calcolata è invece un derivato aggregato, non lega a nessun dato personale del gruppo, coerente con l'uso "solo per comodità di consultazione" dichiarato dall'utente.
- Conseguenze: nessun account creato, nessun push eseguito — restano azioni dell'utente. `git check-ignore -v` conferma: `data/local/fantacalcio.duckdb` incluso, `data/local/ledger.sqlite3` e `data/local/_simulation_ledger.sqlite3` esclusi. 358 test invariati passano (nessuna logica toccata). Prossimo passo (utente): creare repo GitHub privato, pushare, creare account Streamlit Community Cloud, collegare il repo in modalità privata, puntare l'entrypoint su `app/Home.py`.
- Approvato da: project owner

### ADR-2026-049 — Nomi squadra reali versionati in config, seeding idempotente per sopravvivere allo storage effimero del cloud

- Data: 2026-08-16
- Stato: approved
- Supersedes: risolve il problema pratico emerso al primo deploy reale su Streamlit Community Cloud (ADR-2026-048): l'app cloud parte con `data/local/ledger.sqlite3` vuoto (gitignored di proposito), quindi nessuna etichetta squadra
- Scope: UX | operations
- Decisione: assegnati i 20 nomi reali della lega (forniti dall'utente in chat: Garlascow Rangers = squadra dell'utente = `team_01`, già titolare dei 4 lock reali; gli altri 19 assegnati in ordine a `team_02`..`team_20`). Invece di richiedere un reinserimento manuale a ogni riavvio del container cloud, creato `config/team_labels.v1.yaml` (file versionato, a differenza del ledger che resta gitignored) con la mappa `team_id -> nome reale`. Aggiunte due funzioni pure a `src/fantacalcio/persistence/team_labels_store.py`: `load_labels_config()` (legge lo YAML, dict vuoto se il file non esiste) e `seed_missing_labels(conn, mapping)` (assegna un'etichetta **solo** ai `team_id` ancora senza — non sovrascrive mai una rinomina fatta a mano, in sessione locale o cloud). Richiamata a ogni avvio in tutte e 4 le pagine (`Home.py` + le 3 pagine in `app/pages/`), non solo in Home, perché con la navigazione multipagina di Streamlit ogni pagina esegue solo il proprio script — un link diretto a una sotto-pagina senza passare da Home non avrebbe altrimenti mai eseguito il seeding.
- Rationale: lo storage di Streamlit Community Cloud è effimero (ADR-2026-048) — il ledger reale resta giustamente escluso da Git, ma questo significa che ogni riavvio del container azzera anche le etichette squadra, che non sono dati sensibili dell'asta (sono solo nomi di fantasia della lega). Separare "cosa deve restare privato e mai su Git" (ledger, lock) da "cosa può essere versionato perché non è stato live dell'asta" (nomi squadra) risolve il problema senza intaccare la regola di privacy di ADR-2026-048.
- Conseguenze: 8 nuovi test (`tests/test_persistence_team_labels_store.py`, classi `TestLoadLabelsConfig`/`TestSeedMissingLabels`) coprono: caricamento YAML, file mancante, YAML malformato, seeding di un DB vuoto, non-sovrascrittura di un'etichetta già presente, seeding parziale, idempotenza su chiamate ripetute. 365 test totali passano. Verificato in browser (ambiente locale, nessun errore server). Prossimo passo (utente): commit + push di `config/team_labels.v1.yaml` e delle modifiche a `team_labels_store.py`/`app/*.py`, poi redeploy su Streamlit Cloud — i 20 nomi compariranno automaticamente al primo avvio, senza reinserimento manuale.
- Approvato da: project owner

### ADR-2026-050 — Ricerca fuzzy per errori di battitura, nuova pagina Confronto per fase d'asta

- Data: 2026-08-16
- Stato: approved
- Scope: UX
- Decisione: (1) l'utente ha cercato "Dobvyk" (typo di "Dovbyk") nella pagina Giocatori e non ha trovato nulla — la ricerca era solo substring esatta. Aggiunta `search_players_fuzzy()` in `src/fantacalcio/persistence/player_table.py`: fallback automatico quando la ricerca esatta non trova nulla, stesso principio già usato per la risoluzione dei nomi della lista admin (`difflib` + `normalize_name` da `fantacalcio.identity.teams`, soglia minima di somiglianza — mai un match forzato sotto soglia). (2) L'utente ha chiesto una vista per confrontare i giocatori di ciascuna delle 4 fasi d'asta (G1-G4), sia come tabella completa sia come confronto fianco a fianco di pochi giocatori scelti. Nuova pagina `app/pages/4_⚖️_Confronto.py`: stessi filtri di Giocatori (turno/ruolo/squadra/qualità dati) sopra una tabella completa ordinabile, poi un multiselect (max 6) che genera card affiancate (fantavoto, VAR, range, quotazione, rank/punteggio ufficiale se presente). Dichiarato esplicitamente nella pagina che i dati distinguono solo G1/G2/G3_G4 (G3 e G4 condividono lo stesso pool "tutti i rimanenti" per costruzione del motore, `round_pools.py`) — mai finta una separazione G3/G4 che i dati non hanno. Il tetto massimo di offerta (dipende da budget/rosa di una squadra specifica) resta fuori da questa vista comparativa multi-giocatore, con un rimando esplicito alla pagina Giocatori.
- Rationale: la ricerca esatta è troppo fragile sotto pressione (nomi stranieri, battitura veloce durante l'asta); la fuzzy è un fallback, non la sostituisce, così resta prevedibile quando c'è un match esatto. Il confronto multi-giocatore è per natura una vista aggregata su tanti giocatori: mescolarci un tetto di offerta specifico-per-squadra avrebbe richiesto ricalcolarlo per ognuno senza un contesto reale scelto dall'utente, rischiando di mostrare un numero fuorviante — meglio rimandare alla scheda singola già pensata per quello.
- Conseguenze: 5 nuovi test (`tests/test_persistence_player_table.py`, classe `TestSearchPlayersFuzzy`) coprono: match di un typo reale, ordinamento per similarità, query senza alcun match, rispetto degli altri filtri, nessuna colonna di appoggio (`_fuzzy_ratio`) trapelata nell'output. 370 test totali passano. Bug reale trovato e corretto durante la verifica in browser: dopo `set_index("display_name")` il codice provava comunque a leggere `player['display_name']` come colonna nel ciclo delle card — corretto con un `reset_index()` prima del ciclo (il browser ha mostrato subito il `KeyError`, poi confermato risolto). Verificato in browser: ricerca "Dobvyk" trova "Dovbyk"; pagina Confronto renderizza tabella + 3 card di default senza errori.
- Approvato da: project owner

### ADR-2026-051 — Correzione bug di risoluzione per 5 giocatori reali, integrazione dei 4 acquisti genuinamente nuovi con fallback role-average

- Data: 2026-08-16
- Stato: approved
- Supersedes: chiude in modo definitivo il gap sui "9 nuovi giocatori" aperto da ADR-2026-045 — solo 4 dei 9 erano davvero nuovi
- Scope: data | modeling
- Decisione: l'utente ha cercato "Djed Spence" senza trovarlo (davvero assente) e ha chiesto di "implementare tutti i giocatori nuovi". Prima di aggiungere qualunque dato, verificato uno per uno i 9 nomi contro l'anchor (`grep` diretto su `data/staged/fantacalcio_quotazioni_manual/2026_27.csv`), scoprendo che **5 su 9 non erano affatto nuovi**: erano già nel listone reale con un `player_code` valido, ma il resolver automatico li aveva scartati per due ragioni distinte, entrambe legittime da soli:
  - **Isaksen, Rodriguez Je.**: la lista admin li classifica ruolo "A", l'anchor (classificazione classica Fantacalcio) li ha come "C" — disaccordo di ruolo tra due fonti indipendenti, il resolver si rifiuta giustamente di indovinare.
  - **Sucic, Chalobah, Jimenez**: stesso ruolo su entrambe le fonti, ma l'anchor porta un'iniziale disambiguante ("Sucic P.", "Chalobah T.", "Jimenez A.") che spinge il punteggio fuzzy appena sotto la soglia di auto-accettazione (0.8889 / 0.875 contro 0.90) — quasi-match, non un errore.
  Verificati manualmente tutti e 5 (incrocio anchor + fonti web indipendenti sullo stesso giocatore/club) e aggiunto `MANUAL_VERIFIED_CROSSWALK` in `src/fantacalcio/identity/admin_official_list.py` — un crosswalk esplicito, documentato caso per caso, applicato PRIMA della classificazione "nuovo giocatore", senza allentare la soglia/il vincolo di ruolo del resolver automatico per tutti gli altri casi futuri.
  Per i **4 genuinamente nuovi** (Molina N. → Roma, Obrador → Sassuolo, Spence → Inter, Schmid → Frosinone, confermati via ricerca web 2026-08-16, squadra **non verificata** dal file admin, solo da fonti esterne — `team_verified=False`): assegnato un `player_code` negativo (-1..-4, mai potenzialmente in collisione con un ID reale Fantacalcio.it, sempre positivo) tramite `scripts/add_new_signings.py`. Fantavoto/VAR = **media di ruolo del tier `no_history_transfer` già esistente** (stesso identico meccanismo già usato per gli altri trasferimenti senza storico, non una nuova formula), `round_pool`/`list_pool_name` ricalcolati con la funzione reale (`assign_round_pools`) sull'intero pool aggiornato, non indovinati.
- Rationale: un fantavoto "personalizzato" per questi 4 è statisticamente impossibile (zero partite sotto qualunque identità, in qualunque lega tracciata dal sistema) — usare il fallback di ruolo già validato è onesto (CLAUDE.md: mai una falsa precisione) invece di inventare un nuovo meccanismo ad hoc. Un ID positivo "libero" per i 4 nuovi avrebbe rischiato una collisione futura con un vero ID Fantacalcio.it al prossimo aggiornamento del listone — i negativi sono strutturalmente impossibili da confondere con un ID reale.
- Conseguenze: 156 giocatori risolti (151+5), 4 nuovi, coda di revisione **vuota** (0/9 residui, prima 9). Tabella giocatori: 502 righe (498+4), 219 `official`. 1 nuovo test (`test_manual_verified_crosswalk_resolves_role_mismatch_and_near_miss_names`), 371 test totali passano. **Due bug reali trovati e corretti durante l'esecuzione** (non nei test, nell'esecuzione reale degli script): (1) `assign_round_pools()` sovrascrive `list_state` a "provisional" per *tutte* le righe indiscriminatamente — avrebbe cancellato silenziosamente l'overlay ufficiale di ADR-2026-046 sui 215 giocatori già `official`; corretto salvando e ripristinando lo stato originale per player_code. (2) Le righe dei 4 nuovi giocatori non includevano le colonne `used_fvm_prior`/`team_strength_adjustment` presenti nel CSV reale — `pandas` le riempiva con `NA`, che mandava in crash `if player.get("used_fvm_prior"):` nella pagina Giocatori (`TypeError: boolean value of NA is ambiguous`, visto live in browser); corretto popolando i valori reali (uguali per l'intero tier `no_history_transfer`) e aggiunta un'asserzione nello script che blocca preventivamente qualunque futura colonna mancante. Verificato in browser: "Spence" ora si trova, scheda completa senza errori, pagina Confronto invariata.
- Approvato da: project owner

### ADR-2026-052 — Il turno (`round_pool`) segue sempre la admin list, mai il ranking VAR del modello, per ogni giocatore che la lista copre

- Data: 2026-08-16
- Stato: approved
- Scope: data | domain
- Decisione: l'utente ha segnalato che Marcandalli (Lista 4, "41-60 difensori", rank 60 — quindi primo giro/G1 secondo la lista admin) risultava in app con un turno diverso, e ha chiarito che la lista è "chiusa": ogni giocatore che vi compare ha un turno definitivo dettato dalla lista stessa; solo chi non vi compare affatto resta a turno libero. Verificata la causa: `round_pools.assign_round_pools()` calcola `round_pool` (G1/G2/G3_G4) dal ranking VAR del modello, e l'overlay `apply_official_admin_list.py` (ADR-2026-046) copiava `admin_rank`/`admin_score` ma non ricalcolava mai `round_pool` da quello — il turno restava quello del modello anche per giocatori "ufficiali". Corretto con un **hard override esplicito**, coerente con `config/auction_rules.v1.yaml` (`official_pool_authority: admin_import`, `model_ranking_is_official_pool: false`, già presenti ma non implementati fino in fondo) e con l'istruzione diretta dell'utente ("admin data vince sempre"): nuova funzione condivisa `hard_override_round_pool_from_admin_rank()` in `src/fantacalcio/auction/round_pools.py` — per ogni riga D/C/A con un `admin_rank` non nullo entro il cutoff di ruolo della lista (60/60/40, letti da config, mai hardcoded), sovrascrive `round_pool`/`list_pool_name` con il turno della lista admin, **sostituendo** (non solo riempiendo) quello calcolato dal VAR. Usata da entrambi i punti che possono impostare `admin_rank`: `apply_official_admin_list.py` (i 215 giocatori risolti via `player_code`) e `scripts/add_new_signings.py` (i 4 nuovi acquisti senza `player_code` reale, che però hanno comunque un `admin_rank` diretto dalla lista — ADR-2026-051). Durante la verifica trovato e corretto un secondo bug reale: l'ordine in cui giravano gli script faceva sì che il secondo overlay azzerasse a `NaN` l'`admin_rank` dei 4 nuovi acquisti (perché non presenti in `resolved_players.csv`, solo in `new_players.csv`), lasciandoli silenziosamente a turno libero (G3_G4) invece che nel loro turno reale — fissato l'ordine pipeline corretto (overlay ufficiale sui giocatori esistenti prima, poi aggiunta dei nuovi acquisti) e rieseguita da zero su `_m3_replacement_values.csv`/DuckDB.
- Rationale: la missione (`CLAUDE.md`) impone di non confondere mai il ranking del modello con la lista ufficiale, e il config dichiarava già esplicitamente `official_pool_authority: admin_import` — il codice non rispettava ancora quella dichiarazione per la variabile che più conta a un utente in asta dal vivo (in che turno comprare chi). Questa è una correzione di un gap di implementazione rispetto a una decisione di dominio già presa, resa esplicita come ADR perché l'utente l'ha confermata di persona ("admin data vince sempre") dopo aver scoperto il sintomo con un caso reale.
- Conseguenze: verifica completa post-fix su tutti i 219 giocatori `official`: zero mismatch turno/ruolo/cutoff, 59/59 portieri in G1 via blocco club, tutti e 4 i lock reali dell'utente (team_01: Thuram K., Diao, Malen, Carnesecchi) confermati `official` col turno corretto (invariati rispetto a prima). 3 nuovi test in `tests/test_auction_apply_official_admin_list.py` (override applicato, giocatore fuori lista invariato). 373 test totali passano. Marcandalli ora `round_pool=G1` come da lista admin.
- Approvato da: project owner

### ADR-2026-053 — Prezzo pianificato sui lock, usato come controfattuale "cosa mi resta" per la Rosa ideale

- Data: 2026-08-16
- Stato: approved
- Scope: auction | UX
- Decisione: l'utente ha chiesto un controfattuale — dato quanto pensa di pagare i giocatori bloccati, cosa può prendere per riempire il resto della rosa entro budget. Aggiunto un campo `planned_price` (intero, nullable) alla tabella `locks` (`src/fantacalcio/persistence/locks_store.py`, migrazione `ALTER TABLE` additiva su DB pre-esistenti, mai un valore indovinato per i lock già salvati). È **l'ipotesi dell'utente**, non un'offerta né un output del modello: può stare sopra o sotto la quotazione, a differenza dei candidati proposti dall'ottimizzatore (vedi sotto). `app/pages/3_⭐_Rosa.py` ("Rosa ideale", già esistente da M4 slice 6/ADR precedenti) ora usa `planned_price` (fallback a quotazione se non impostato) invece della sola quotazione per calcolare budget/slot già impegnati dai lock prima di chiamare `optimize_roster_completion()` — nessun nuovo motore, solo wiring del dato giusto in quello già esistente.
  Vincolo aggiuntivo esplicito dell'utente: i candidati **non** bloccati proposti dall'ottimizzatore non possono mai costare meno della loro quotazione — in G1/G2 quello è il minimo d'offerta di regolamento (ADR-2026-013), quindi un costo più basso descriverebbe un'offerta non valida, non solo un'ipotesi ottimistica. Nuova funzione pura `candidate_price_floor()` in `src/fantacalcio/auction/roster_optimizer.py`: `max(admin_quotazione, quotazione_asta)` se esiste una quotazione admin per-giocatore, altrimenti `quotazione_asta`. Verificato nel codice che oggi **non esiste** ancora una quotazione admin per-giocatore (solo `admin_rank`/`admin_score`/blocco portieri per club, `apply_official_admin_list.py`) — la funzione collassa quindi oggi su `quotazione_asta` (comportamento identico a prima), pronta senza modifiche per quando arriverà.
- Rationale: separare sempre acquistato/ipotetico/vincolo di regolamento (`CLAUDE.md`), invece di far collassare "quanto vorrei pagare" e "quanto posso legalmente offrire" nello stesso numero.
- Conseguenze: 3 nuovi test su `locks_store` (default `None`, aggiornabile, migrazione su DB con schema vecchio) + 3 su `candidate_price_floor`. 379 test totali passano. Verificato in browser sul DB reale dell'utente: migrazione automatica sui lock esistenti (fallback a quotazione, "Costo pianificato totale dei lock: 71 crediti"), form di lock con nuovo campo "Prezzo pianificato", "Calcola rosa ideale" funzionante senza errori col floor applicato.
- Approvato da: project owner

### ADR-2026-054 — La quotazione admin per-giocatore sostituisce (non affianca) la quotazione fantacalcio ovunque in UI

- Data: 2026-08-16
- Stato: approved
- Scope: auction | UX
- Decisione: l'utente ha chiarito, riallegando `Liste Fantacalcio 26_27.md`, che il numero accanto a ogni giocatore nella lista admin (es. "1. Dimarco 55") **è** una quotazione — non un semplice punteggio di ranking. Verificato nel codice: quel numero era già importato come `admin_score` da `apply_official_admin_list.py` (ADR-2026-046), presente in `_m3_replacement_values.csv` e quindi in tutta la tabella DuckDB `players`, ma finora **mai usato come prezzo** — solo mostrato come metadato separato ("Punteggio admin") accanto alla quotazione fantacalcio (`quotazione_asta`), le due mai in conflitto perché mai confrontate. La config dichiarava già l'intento (`minimum_bid_source: list_stated_minimum` per G1/G2, `config/auction_rules.v1.yaml:54,66`) senza che il codice lo rispettasse fino in fondo — stesso pattern già corretto per `round_pool` in ADR-2026-052.
  Nuova funzione pura `effective_quotazione()` in `src/fantacalcio/persistence/player_table.py`: ritorna `admin_score` quando presente (giocatore `official`), altrimenti `quotazione_asta` — mai i due sommati o mediati, un rimpiazzo netto, coerente con "admin data vince sempre" (istruzione diretta dell'utente, già citata in ADR-2026-052). Applicata ovunque la quotazione compare in UI: `app/pages/1_🔍_Giocatori.py` (tabella risultati, scheda giocatore), `app/pages/4_⚖️_Confronto.py` (tabella, confronto fianco a fianco — rimossa la colonna "Punteggio admin" separata, ora ridondante), `app/pages/3_⭐_Rosa.py` (tabella lock, calcolo budget "Rosa ideale"). Anche `candidate_price_floor()` (ADR-2026-053) ora riceve `admin_score` come `admin_quotazione` quando disponibile, invece di `None` fisso.
- Rationale: la lista admin è la fonte di verità della vera asta (`CLAUDE.md`); mostrare due numeri diversi per "quanto costa questo giocatore" quando uno dei due non è più valido per la fase G1/G2 rischiava di far usare all'utente il numero sbagliato in un contesto ad alta pressione (asta dal vivo). Nessuna nuova formula: solo selezione esplicita tra due valori già calcolati altrove, nel rispetto di "la UI non duplica formule del motore".
- Conseguenze: 4 nuovi test su `effective_quotazione` (fallback su None, su NaN, su admin_score presente anche se più basso della quotazione fantacalcio, su colonna assente). 383 test totali passano. Verificato in browser sui dati reali: Martinez L. (quotazione fantacalcio 35, admin_score 74, `official`) mostra correttamente **74** in scheda giocatore, non più 35.
- Approvato da: project owner

### ADR-2026-055 — Import dei risultati reali G1 (portieri+difensori) dal recap admin; il blocco portieri può essere sotto la dimensione configurata

- Data: 2026-08-17
- Stato: approved
- Scope: auction | domain
- Decisione: l'utente ha fornito `Riepilogo primo giro asta.xlsx` (recap admin del primo giro d'asta reale, 20 squadre). Formato: Foglio1 una riga per squadra con 4 coppie (nome, prezzo) — la prima è il **club** il cui blocco portieri è stato comprato (es. "Atalanta" 55), le altre tre sono difensori nominali; Foglio2 lo stesso elenco appiattito, ordinato per prezzo. Colonna budget residuo G1 e budget disponibile G2 (=residuo+100) verificate coerenti con `AUCTION_RULES.md`. Scritto `scripts/import_g1_results.py`: risolve ogni nome squadra libero contro `config/team_labels.v1.yaml` (match esatto poi fuzzy), ogni nome giocatore contro `search_players`/`search_players_fuzzy` (mai un match forzato sotto soglia, stesso principio del resolver lista admin), ogni blocco portieri contro i 3 `player_code` con `list_pool_name="goalkeeper_blocks"` del club. Valida l'intero batch con `replay()` prima di scrivere, dry-run di default (`--yes` per scrivere davvero).
  Durante la risoluzione, scoperti 2 club (Cagliari, Lecce) con **solo 2 portieri reali** censiti nel DB (non 3) — carenza dati già nota da una simulazione precedente (`docs/CURRENT_TASK.md`, "Cagliari/Lecce senza blocco portieri monoclub possibile"), ora confermata reale in un'asta vera. Il vincolo di dominio (`src/fantacalcio/domain.py`) imponeva `len(player_ids) == goalkeeper_block_size` in modo rigido: un blocco da 2 avrebbe fatto fallire ogni `replay()` successivo, cioè l'intera app, non solo l'import. Confermato con l'utente e corretto il vincolo: `len(player_ids) > goalkeeper_block_size` è ancora un errore (bug reale, sempre), ma `< goalkeeper_block_size` è ora permesso — riflette la realtà (alcuni club non hanno 3 portieri validi in lista), non un rilassamento arbitrario della regola. Il quarto portiere in eccesso per Sassuolo (4 nel DB, block reale da 3) escluso per `var_mean` più basso (Russo A.), scelta esplicita dell'utente perché non verificabile contro un dato admin diretto.
- Rationale: i dati reali di un'asta dal vivo espongono lacune del pool sottostante che nessuna euristica dovrebbe nascondere silenziosamente (`CLAUDE.md`: mai spesa/assegnazione che non rifletta la realtà). Il fix di dominio è minimo e conservativo: mantiene la protezione reale (blocco sovradimensionato = bug) e rimuove solo un vincolo che non corrispondeva ai dati disponibili.
- Conseguenze: 80 eventi (`AssignmentEvent`, `source="admin_g1_recap_xlsx_import"`) scritti sul ledger reale, replay coerente con l'Excel (es. team_01: 178 speso su 203 disponibili = 200+3 bonus logo, budget G2 122 = 22 residuo+100). `test_goalkeeper_block_wrong_size_raises` sostituito da `test_goalkeeper_block_oversized_raises` + `test_goalkeeper_block_undersized_is_allowed` + `test_goalkeeper_block_empty_raises_at_construction` in `tests/test_domain_replay.py`. 385 test totali passano. Verificato in browser: pagina Squadre carica senza errori con gli 81 eventi reali nel ledger (80 G1 + 1 bonus logo pre-esistente).
- Approvato da: project owner

### ADR-2026-056 — Modulo "Mercato": profilo squadre avversarie e inflazione per ruolo dopo un turno chiuso

- Data: 2026-08-17
- Stato: approved
- Scope: auction | UX
- Decisione: l'utente ha chiesto un modo per leggere, dopo un turno chiuso (G1 ora, G2/G3/G4 in futuro), come si comporteranno probabilmente le altre squadre — chi ha slot scoperti, chi ha molto budget libero, chi ha comprato solo riserve — per aggiustare le proprie offerte nei turni successivi, con l'aspettativa esplicita che il modulo si riapplichi da solo man mano che arrivano i dati dei turni seguenti (non un'analisi una tantum).
  Nuovo modulo puro `src/fantacalcio/auction/market_model.py`, che riusa `remaining_roster_slots()`/`budget_remaining_for_round()` già esistenti in `bid_recommendation.py` invece di duplicarli:
  - `opponent_profile()`: per squadra e turno, slot ancora scoperti per ruolo, budget residuo diviso per gli slot scoperti (proxy di "margine per un'offerta aggressiva"), e un **segnale di qualità** per ruolo — differenza tra la quotazione media (`effective_quotazione`, ADR-2026-054) di quanto comprato finora e la quotazione media dell'intero pool di quel ruolo: positivo = ha comprato sopra la media (probabile titolare), negativo = sotto (probabile riserva, plausibile che rientri per un titolare). Nessun valore quando la squadra non ha ancora comprato in quel ruolo (mai uno zero inventato al posto di "nessun dato").
  - `round_role_inflation()`: rapporto medio prezzo pagato / quotazione attesa per ruolo, sulle sole compravendite individuali del turno (`effective_events()`, quindi correzioni/annullamenti mai contati due volte); i blocchi portiere sono esclusi perché il prezzo è per l'intero blocco, non per singolo giocatore, non confrontabile con una quotazione individuale. Riportato sempre come intervallo (min–max osservato) + conteggio osservazioni, mai un numero secco: sotto `MIN_RELIABLE_SAMPLE=8` osservazioni la UI mostra un avviso esplicito invece di trattare la stima come affidabile — istruzione diretta dell'utente ("intervallo con avviso pochi dati", non solo direzione qualitativa).
  Nuova pagina `app/pages/5_📊_Mercato.py` (stessa struttura/pattern delle altre pagine `Squadre`/`Rosa`): selettore di turno chiuso, tabellone per le 19 squadre avversarie, sezione inflazione per ruolo. Esplicitamente descrittivo, non un'offerta pronta — non tocca `bid_recommendation.py`, che resta l'unico punto che raccomanda un numero da offrire; l'utente ha scelto una pagina dedicata invece di innestare i segnali dentro le pagine esistenti, per poterla consultare come lettura a sé prima di decidere.
- Rationale: `CLAUDE.md` impone di modellare la domanda avversaria (scarsità, budget ombra, probabilità di concorrenza) e di mostrare sempre distribuzioni/incertezza, mai falsa precisione — con soli ~20-60 osservazioni per turno un numero secco di inflazione sarebbe fuorviante in una situazione ad alta pressione (asta dal vivo). Riusare le funzioni di `bid_recommendation.py` invece di ricalcolare slot/budget mantiene un'unica fonte di verità per quei numeri.
- Conseguenze: 10 nuovi test in `tests/test_auction_market_model.py` (profilo con slot/budget/qualità, rosa piena → nessun budget/slot, nessun acquisto → nessun segnale qualità, inflazione con soglia di affidabilità, blocco portieri escluso, turno sbagliato escluso, evento annullato escluso). 395 test totali passano. Verificato in browser sui dati reali G1 (importati in ADR-2026-055): inflazione difensori 1.23× medio, range 1.00×–1.77×, n=60 (20 squadre × 3 difensori), marcata "stabile" avendo superato la soglia minima; pagina carica senza errori, tabellone squadre e pulsante export CSV presenti.
- Approvato da: project owner

### ADR-2026-057 — Correzione di inflazione (mercato) agganciata al massimo consigliato, breakdown passo-per-passo sempre mostrato

- Data: 2026-08-17
- Stato: approved
- Scope: auction | UX
- Decisione: l'utente ha chiesto di collegare i segnali del modulo Mercato (ADR-2026-056) al massimo consigliato (`recommend_max_bid`, `bid_recommendation.py`) — non solo mostrarli a sé, ma correggere il numero — con la condizione esplicita che ogni volta compaia il breakdown di come il prezzo viene fuori.
  `recommend_max_bid()` accetta ora parametri opzionali (`voti_role`, `player_conn`, `all_events`, `opponent_ids`), tutti con default `None`: se omessi il comportamento è identico a prima (stessi 20 test esistenti passano invariati, nessun cambio di firma obbligatorio). Quando forniti:
  - **Inflazione**: nuova `market_model.role_price_inflation()` — rapporto medio prezzo/quotazione per lo stesso ruolo, aggregato su **tutti i turni già chiusi tranne quello su cui si sta offrendo** (`exclude_round_id`), non solo l'ultimo — così un'offerta in G3/G4 (pool misto) può usare sia l'inflazione difensori di G1 sia quella centrocampisti/attaccanti di G2. Se non esiste ancora nessun dato storico per quel ruolo (es. G1 per gli attaccanti, mai prezzati prima), **nessuna correzione applicata**, esplicitato nel breakdown — mai un fattore inventato in assenza di dati.
  - **Concorrenza**: nuova `market_model.opponents_needing_role()` — quante squadre avversarie hanno ancora uno slot scoperto in quel ruolo, e il loro budget medio/slot. Mostrata sempre come segnale informativo nel breakdown e in UI, **mai moltiplicata nel prezzo**: trasformare "N avversari ne hanno bisogno" in un rialzo numerico del prezzo è una decisione di strategia reale (di quanto? con quale formula?) che richiederebbe una propria approvazione esplicita — inventarla ora avrebbe violato la stessa regola (`CLAUDE.md`: mai un tie-breaker/formula inventata senza approvazione) già rispettata altrove nel progetto (`resolve_sealed_bid_round`).
  Ogni chiamata produce ora un campo `explanation: tuple[str,...]` con ogni passaggio del calcolo in ordine (budget, slot, riserva, discrezionale, quota VAR, prezzo base, correzione inflazione o sua assenza, segnale di concorrenza, prezzo finale) — sostituisce il markdown hardcoded che la pagina Giocatori costruiva a mano, così il breakdown mostrato in UI è generato dalla stessa funzione che calcola il numero, non ricostruito separatamente (rischio di disallineamento). `app/pages/1_🔍_Giocatori.py` passa ora `voti_role`/`player_conn`/`all_events`/`opponent_ids` e mostra `rec.explanation` riga per riga nell'expander "Come si calcola questo numero?", più una didascalia sintetica sulla concorrenza sopra il tetto consigliato.
- Rationale: la missione (`CLAUDE.md`) impone esplicitamente che il bid/value includa "budget ombra, ... probabilità di concorrenza" — questo era l'unico pezzo della lista ancora esplicitamente rimandato (docstring di `bid_recommendation.py`, "deliberately NOT included yet"). L'inflazione è una correzione empirica difendibile (rapporto osservato prezzo/quotazione); la concorrenza resta solo informativa perché tradurla in un numero richiede una regola scelta dall'utente, non improvvisata.
- Conseguenze: 4 nuovi test in `tests/test_auction_bid_recommendation.py::TestMarketContext` (nessun contesto → invariato, inflazione applicata correttamente con moltiplicatore verificato, turno corrente escluso dalla base storica, concorrenza riportata ma non moltiplicata nel prezzo). 399 test totali passano. Verificato in browser sui dati reali G1: scheda giocatore mostra il breakdown completo passo-per-passo, corretta assenza di correzione per un attaccante (nessun dato G1 per quel ruolo) e segnale di concorrenza (19/19 squadre con slot attaccante scoperto, budget medio/slot 10.8) mostrato come informativo.
- Approvato da: project owner

### ADR-2026-058 — Stima di mercato "a cascata" (fascia di prezzo, ruolo, regime generale, aggressività per squadra) al posto di un moltiplicatore unico per ruolo

- Data: 2026-08-17
- Stato: approved
- Scope: auction | domain
- Decisione: l'utente ha chiesto che i dati di un turno chiuso servano a stimare i turni successivi anche quando questi coinvolgono **ruoli mai prezzati prima** (G1 prezza solo portieri/difensori, G2 introduce centrocampisti/attaccanti mai visti), e che la correzione non fosse un singolo moltiplicatore per ruolo ma un'analisi più specifica. Chiarito con l'utente (3 direzioni proposte, scelte tutte e tre): comportamento delle squadre trasversale ai ruoli, segmentazione per fascia di prezzo, regime di mercato generale.
  `market_model.py` guadagna quattro livelli di stima, dal più specifico al più generico, usati in cascata da una nuova `estimate_price_correction()`:
  1. **Ruolo + fascia di quotazione** (`price_tier_inflation(voti_role=...)`): le compravendite storiche dello stesso ruolo divise in terzili di quotazione (`pandas.qcut`) — riflette che i giocatori economici spesso vengono pagati molto più sopra quotazione di quelli costosi (effetto offerta minima), un pattern reale che un unico numero per ruolo nasconde.
  2. **Ruolo intero** (`role_price_inflation()`, già esistente da ADR-2026-057): fallback quando la fascia specifica non ha abbastanza dati (`MIN_SAMPLE_FOR_SPLIT=6`).
  3. **Fascia di prezzo, ruoli misti** (`price_tier_inflation(voti_role=None)`): il pattern generale "chi costa poco viene pagato di più in proporzione", raccolto su tutti i ruoli insieme — usabile per un ruolo mai prezzato (G2 dai dati G1) perché la forma della curva (non il livello) è plausibilmente simile tra ruoli.
  4. **Regime di mercato generale** (`market_regime_ratio()`): un solo rapporto prezzo/quotazione su ogni acquisto storico, blocchi portiere inclusi (prezzati contro la somma delle quotazioni dei 3 portieri) — il fallback più grezzo ma sempre disponibile appena esiste una sola compravendita in tutta la lega.
  La cascata sceglie il livello più specifico **affidabile** (`n >= MIN_RELIABLE_SAMPLE`); se nessuno è affidabile, usa il più specifico comunque disponibile, sempre marcato come poco affidabile — mai un livello nascosto, mai due livelli mescolati in un numero opaco. `PriceCorrection.source` nomina sempre quale livello è stato usato, ed è quello che compare nel breakdown passo-per-passo già introdotto in ADR-2026-057.
  Nuovo, separato: `team_aggressiveness_index()` — per ogni squadra, quanto il suo rapporto prezzo/quotazione medio (su **tutti** gli acquisti fatti finora, qualsiasi ruolo/turno) si discosta dalla media di lega. È un segnale di stile di offerta che attraversa i ruoli (una squadra che ha pagato sopra quotazione i difensori in G1 è plausibilmente una squadra che tende a pagare sopra quotazione in generale, non specificamente sui difensori) — mostrato in una nuova colonna "Stile di offerta" nel tabellone squadre della pagina Mercato, insieme a una sezione "Regime di mercato generale" e una tabella per-fascia per ogni ruolo con dati storici.
- Rationale: un solo moltiplicatore per ruolo, oltre a essere ciò che l'utente ha esplicitamente chiesto di superare, non si sarebbe comunque potuto applicare a un ruolo mai prezzato — la cascata risolve proprio questo, degradando in modo esplicito e trasparente verso segnali via via più generici invece di restituire "nessun dato" ogni volta che manca la granularità massima.
- Conseguenze: 10 nuovi test in `tests/test_auction_market_model.py` (fasce con ratio diversi per fascia economica/costosa, troppo pochi dati → fasce vuote, regime che include i blocchi portiere, aggressività positiva/negativa, cascata che degrada correttamente da fascia a ruolo a regime generale). `bid_recommendation.py` ora usa `estimate_price_correction()` al posto del solo `role_price_inflation()`; 1 test esistente aggiornato per la nuova etichetta nel breakdown. 408 test totali passano. Verificato in browser sui dati reali G1: regime di mercato generale 1.49× (tutti i ruoli inclusi i blocchi portiere), fascia difensori popolata, vista per-turno 1.23× (coerente con prima), nessun errore.
- Approvato da: project owner

### ADR-2026-059 — Import/export manuale del ledger + seeding automatico idempotente da Streamlit Secrets

- Data: 2026-08-17
- Stato: approved
- Scope: operations | UX
- Decisione: l'utente ha scoperto che l'app pubblicata su Streamlit Community Cloud parte con un ledger vuoto (storage effimero, ADR-2026-048) e ha chiesto se i dati reali potessero esserci "senza doverli caricare io" a ogni riavvio. Non è possibile automatizzare interamente: l'assistente non può accedere all'account/dashboard Streamlit Cloud dell'utente (creare account, entrare in impostazioni altrui sono azioni vietate). Implementate due parti distinte:
  1. **Trasferimento manuale, una tantum per sessione**: `fantacalcio.ledger_io.import_ledger_json_text()` (nuova, stessa logica di `import_ledger_json` ma da stringa in memoria invece che da path) + sezione "Importa / esporta ledger" in `app/pages/2_📋_Squadre.py` — download del ledger corrente come JSON, upload di un JSON esportato da un'altra istanza, con merge per `event_id` (solo eventi nuovi, mai duplicati) validato via `replay()` prima di scrivere. Il trasferimento resta sempre browser↔server, mai attraverso Git.
  2. **Seeding automatico e idempotente ai riavvii successivi**: nuove `ledger_store.seed_missing_events_from_secrets()` / `seed_missing_events_from_streamlit_secrets()` — leggono un export JSON dalla chiave `ledger_seed_json` di **Streamlit Secrets** (mai committata su Git, impostata una sola volta dall'utente stesso nella dashboard Cloud, azione sua non dell'assistente) e appendono solo gli eventi non ancora presenti, validati via `replay()` prima di scrivere; sollevano `SeedFromSecretsError` (mai un fallimento silenzioso) se il secret è malformato o violerebbe un invariante. Richiamata a ogni caricamento delle pagine che usano il ledger (Giocatori, Squadre, Rosa, Mercato), stesso pattern già in uso per `seed_missing_labels()` (ADR-2026-049): dopo che l'utente incolla l'export una sola volta nei Secrets del progetto Cloud, ogni riavvio del container si riseeda da solo, senza upload manuale ripetuto.
- Rationale: il ledger reale resta correttamente escluso da Git per privacy (ADR-2026-048) — nessuna di queste due funzionalità lo aggira, entrambe passano solo per canali che l'utente controlla direttamente (upload nel browser, o Secrets della propria dashboard Cloud). L'assistente non può eseguire l'azione "incollare nei Secrets" al posto dell'utente: è esplicitamente un'azione di configurazione account riservata all'utente.
- Conseguenze: 3 nuovi test in `tests/test_ledger_io.py` (roundtrip da stringa, JSON malformato, root non-lista) + 6 nuovi in `tests/test_persistence_ledger_store.py` (no-op su secret assente, seeding, idempotenza, solo eventi mancanti, JSON malformato solleva errore, violazione di invariante non scrive nulla). 417 test totali passano. Verificato in browser: sezione import/export visibile e funzionante nella pagina Squadre, nessun errore su un'istanza locale senza Secrets configurati (percorso no-op silenzioso, comportamento invariato). Esportato il ledger reale dell'utente (81 eventi) e consegnato come file per il primo upload manuale.
- Approvato da: project owner

### ADR-2026-060 — G2 centrocampisti/attaccanti a fasce da 20, non un pool unico per ruolo

- Data: 2026-08-17
- Stato: approved
- Supersedes: parte "G2" di ADR-2026-013 (che assumeva, senza verifica esplicita, un'unica lista da 6 preferenze per ruolo in G2 come in G1)
- Scope: auction
- Decisione: in G2 i 60 centrocampisti e i 40 attaccanti dell'admin sono numerati e divisi in fasce da 20 (centrocampisti: 1-20, 21-40, 41-60; attaccanti: 1-20, 21-40); ogni fascia è una busta indipendente con le proprie 6 preferenze e la propria risoluzione preferenza-poi-offerta — non un'unica lista da 6 preferenze per l'intero pool di ruolo come in G1 (blocco portieri, top-60 difensori). Confermato dall'utente in chat (non nel recap admin scritto, che non specificava questo dettaglio).
- Rationale: l'utente ha segnalato la discrepanza chiedendo aiuto per comporre "sestetti" per 3 gruppi di centrocampisti; senza questa correzione `config/auction_rules.v1.yaml` avrebbe validato/interpretato G2 in modo strutturalmente diverso da come si gioca davvero (un pool unico invece di 3+2 buste separate), con impatto diretto su quante offerte si possono davvero fare in G2 e su come si calcola il budget residuo per fascia.
- Conseguenze: `config/auction_rules.v1.yaml` (`pools` di G2: `midfielders_top_1_20/21_40/41_60`, `forwards_top_1_20/21_40` al posto dei due pool unici `midfielders_top_1_60`/`forwards_top_1_40`); `src/fantacalcio/domain.py` (`_KNOWN_POOL_ROLES` aggiornato); `src/fantacalcio/auction/round_pools.py` (nuova logica a fasce in `assign_round_pools`/`hard_override_round_pool_from_admin_rank`, pareggi al cutoff totale confluiscono nell'ultima fascia reale, stessa policy "nessun tie-break inventato" già in uso); `src/fantacalcio/fixtures.py` e test esistenti aggiornati ai nuovi nomi pool; 4 nuovi test in `tests/test_auction_round_pools.py`. `docs/AUCTION_RULES.md` aggiornato. 420 test totali passano. G1 (già concluso nella realtà, 81 eventi nel ledger) non viene retroattivamente ricontrollato per lo stesso pattern a fasce — se anche G1 avesse usato fasce da 20 sui difensori, è un fatto storico ormai chiuso, non azionabile.
- Approvato da: project owner

### ADR-2026-061 — Pagina "Buste G2": composizione e verifica di fattibilità delle 5 buste, bozza persistente

- Data: 2026-08-17
- Stato: approved
- Scope: auction | UX
- Decisione: nuova pagina `app/pages/6_📝_Buste_G2.py` per comporre le 5 buste G2 (ADR-2026-060: 3 fasce centrocampisti + 2 fasce attaccanti) con multiselect filtrato per fascia (solo giocatori con `admin_rank` nella fascia, mai un fallback VAR silenzioso), preferenza 1-6 e offerta per giocatore. Le bozze sono persistenti tra sessioni in una nuova tabella SQLite `g2_envelope_picks` (`src/fantacalcio/persistence/g2_envelopes_store.py`, stesso pattern di `locks_store.py`): intento pianificatorio, mai scritto nel ledger reale, zero effetto su budget/rosa finché l'esito non viene registrato davvero in Squadre. La verifica di fattibilità (`src/fantacalcio/auction/g2_envelope_feasibility.py`) modella la spesa nel caso peggiore come la **somma delle offerte massime per fascia**, non la somma di tutte le offerte: entro una fascia si vince al più un giocatore (quello con la preferenza più alta che risolve a favore), quindi il costo peggiore di quella fascia è la sua offerta più alta, non la somma delle 6.
- Rationale: l'utente ha chiesto esplicitamente di testare la composizione delle buste direttamente nell'app invece che via script ad-hoc in chat, dopo aver verificato manualmente budget/fasce reali dal ledger (25 crediti residui G1, 125 disponibili G2, rosa 3P+3D+0C+0A). Filtrare solo per `admin_rank` (non VAR) è la correzione esplicita dell'utente alla sessione: un giocatore senza `admin_rank` noto in quella fascia non è ancora confermato lì, quindi `check_pick_feasibility` blocca l'inserimento con motivo esplicito invece di lasciarlo entrare su una posizione di fascia indovinata.
- Conseguenze: 5 nuovi test in `tests/test_persistence_g2_envelopes_store.py`, 13 nuovi in `tests/test_auction_g2_envelope_feasibility.py`. 438 test totali passano. `data/staged/fantacalcio_voti_manual/_m3_replacement_values.csv` e `data/local/fantacalcio.duckdb` rigenerati (`scripts/run_m3_replacement_values.py` + `scripts/apply_admin_official_list.py`) per riflettere le nuove fasce prima di verificare la pagina in browser: 5 fasce G2 popolate (23-28 giocatori "in lizza" tra VAR provvisorio e admin ufficiale per fascia, non esattamente 20 — atteso, i due ranking non coincidono, ADR-2026-021/054), 215/498 giocatori ufficiali. Verificato in browser end-to-end: budget G2 letto dal ledger reale (125), aggiunta di una preferenza reale (Paz N., fascia centrocampisti 1-20, 30 crediti) ricalcola margine 125→95 immediatamente, rimozione funzionante; dato di test poi ripulito dal DB reale. Fuori scope, lasciato per una richiesta futura: controllo di fattibilità che incroci le 5 buste con i lock/rosa-ideale già esistenti in Rosa, e reserve congiunta multi-round (oggi il margine è solo su G2, non incorpora la riserva minima per G3/G4).
- Approvato da: project owner

### ADR-2026-062 — Proiezione di budget G3/G4 dopo lo scenario G2, con minimo reale (quotazione, non 1 credito)

- Data: 2026-08-17
- Stato: approved
- Scope: auction | UX
- Decisione: estesa la pagina Buste G2 con una proiezione "e dopo G2?" su due scenari (caso peggiore = offerta più alta di ogni fascia vince; tutte-prime-scelte = la preferenza #1 di ogni fascia vince), che calcola il budget G3+G4 residuo (`remaining_G2 + budget_increment di G3`, letto da config, mai 40 hardcoded) e lo confronta col **minimo reale** necessario per completare la rosa: la somma delle quotazioni dei N giocatori più economici ancora liberi per gli N slot ancora scoperti — non 1 credito a slot, perché in G3/G4 `minimum_bid_source: player_quotazione` (a differenza di G1/G2 dove il minimo è autodichiarato in lista). Se il margine è negativo, la UI segnala esplicitamente "sei in difficoltà" invece di un generico avviso.
- Rationale: l'utente ha segnalato budget molto stretto e ha chiesto di vedere, per ogni fase, cosa resterebbe vincendo tutte le prime scelte — non bastava più il solo margine G2 già implementato in ADR-2026-061, serviva la proiezione a valle perché G3/G4 hanno un vincolo di minimo diverso (quotazione reale) che un controllo basato solo su G2 nasconderebbe.
- Conseguenze: `src/fantacalcio/auction/g2_envelope_feasibility.py` esteso (`BandSpend.first_choice_bid`/`first_choice_player_code`, `G2FeasibilityReport.first_choice_total_spend`/`first_choice_margin`, nuova funzione `project_downstream_budget` → `DownstreamProjection`); `app/pages/6_📝_Buste_G2.py` mostra entrambi gli scenari con budget G3+G4, minimo richiesto e margine, spiegazione passo-per-passo. 6 nuovi test in `tests/test_auction_g2_envelope_feasibility.py` (spesa prima-scelta vs caso-peggiore, budget G3+G4 corretto, minimo da quotazioni reali, esclusione dei giocatori vinti nello scenario, sforamento rilevato, margine positivo con giocatori economici). 444 test totali passano. Verificato in browser sui dati reali: con busta vuota, budget G3+G4 proiettato = 165, minimo per 18 slot ancora scoperti = 18, margine 147 (OK) — numero che si restringerà correttamente man mano che l'utente inserisce offerte reali nelle 5 buste.
- Approvato da: project owner

### ADR-2026-063 — Import risultati reali G2, correzione bonus logo mancante su 19/20 squadre, offerte fallite escluse dal ledger per scelta esplicita

- Data: 2026-08-18
- Stato: approved
- Scope: auction | domain | data-quality
- Decisione: l'utente ha fornito `Riepilogo secondo giro asta.xlsx` (recap admin cumulativo G1+G2, 9 coppie giocatore/costo per squadra) e le 9 "Lista N (Risposte).xlsx" (una riga per squadra con le 6 preferenze reali per fascia G2, comprese quelle fallite). `scripts/import_g2_results.py` scrive solo i 5 pair G2 nuovi (le colonne G1 sono già a ledger da ADR-2026-055); il pool di ogni evento è risolto dal `list_pool_name` del giocatore risolto, non dalla posizione di colonna. Celle gialle/viola nel recap = assegnazione manuale automatica dell'admin (confermato dall'utente), scritte come evento normale con `source` distinto (`..._manual_auto_assignment`) per tracciabilità, mai un flag di dominio separato.
  Validando il replay è emerso un errore reale del ledger: il bonus logo (+3 crediti, ADR-2026-013) era registrato solo per `team_01`, ma i totali del recap G2 implicano budget G1=203 per **tutte e 20** le squadre. Confermato dall'utente: tutte le squadre avevano un logo personalizzato. `scripts/backfill_logo_bonus_all_teams.py` ha scritto i 19 `BudgetAdjustmentEvent` mancanti (dry-run validato via replay prima della scrittura).
  Risoluzione nomi: alcuni nomi nel recap erano ambigui o corrotti (mojibake per lettere accentate, ordine nome/cognome invertito, refusi). Ogni caso ambiguo è stato risolto con conferma esplicita dell'utente (mai un match forzato sotto soglia): "Adams" → Adams A. (Venezia, non Adams C. Torino), "Koné" → Koné M. (fascia 1-20, non Koné I. 21-40 né Koné B. fuori lista), "Carlos K"/"K. Davis"/"lautaro"/"Bernabe"/"Gonçalo Ramos"/mojibake "Soulé" risolti per singolo match non ambiguo dopo normalizzazione ASCII della query. "Schmid" (Joga Benito) non risolto — nessun centrocampista con quel nome nel player table, l'utente ha chiesto di saltarlo e verificarlo a parte: **evento non scritto**, resta un gap noto da chiudere in futuro.
  Le liste di preferenza complete (comprese le offerte perse/mai valutate) sono usate **solo** per arricchire `market_model.py` (comportamento/aggressività delle squadre avversarie), esplicitamente non per scrivere eventi sul ledger né per inferire domanda futura su singoli giocatori — scelta confermata dall'utente.
- Rationale: `CLAUDE.md` vieta ogni assegnazione/spesa che non rifletta la realtà e ogni join per solo nome senza verifica; il gap del bonus logo, se lasciato, avrebbe fatto fallire il replay per quasi ogni squadra man mano che i dati G2 reali venivano importati (budget insufficiente fittizio), un sintomo del problema di fondo (dato G1 incompleto) non un errore dell'import G2.
- Conseguenze: 99 nuovi `AssignmentEvent` (G2) + 19 `BudgetAdjustmentEvent` (backfill bonus logo) scritti sul ledger reale. Nessun nuovo test unitario (pattern esistente: gli script di import one-off admin non hanno una suite dedicata, come `import_g1_results.py`); validazione tramite dry-run + `replay()` prima di ogni scrittura, come da script precedenti. Gap noto e non bloccante: "Schmid" (Joga Benito, ~21 crediti) da risolvere manualmente.
- Approvato da: project owner

### ADR-2026-064 — Schmid/Obrador (giocatori ufficiali senza player_code) reintegrati; evento G2 mancante scritto

- Data: 2026-08-18
- Stato: approved
- Scope: data-quality | auction
- Decisione: durante l'import G2 (ADR-2026-063), "Schmid" (centrocampista, rank 43 lista admin) non risultava in nessun match, nemmeno fuzzy, nel player table. Verifica richiesta dall'utente: `scripts/add_new_signings.py` esisteva già (4 giocatori: Molina N., Obrador, Spence, Schmid, con `player_code` negativo dedicato) ma non era mai stato ri-eseguito dopo che `_m3_replacement_values.csv` era stato rigenerato da zero per le fasce G2 (ADR-2026-061) — la rigenerazione aveva cancellato silenziosamente le righe con codice negativo aggiunte in precedenza. Di questi 4, Molina N. e Spence risultano nel frattempo risolti con un `player_code` reale positivo tramite il normale matching di identità (l'entry in `new_players.csv` era ormai stale per loro); solo Schmid e Obrador restavano davvero senza codice. Ri-eseguiti in ordine `add_new_signings.py` → `apply_admin_official_list.py` per ricostruire CSV e DuckDB con i due giocatori mancanti (player_code -4 e -2, quotazione = punteggio lista admin, nessuno storico reale disponibile → fallback `no_history_transfer` sulla media di ruolo, stesso pattern già approvato in ADR-2026-051). Scritto poi il singolo `AssignmentEvent` G2 mancante per Schmid (team_07/Joga Benito, 21 crediti, midfielders_top_41_60).
- Rationale: il gap non era un problema di risoluzione nomi di questa sessione ma una regressione di una pipeline già corretta in passato, silenziata da uno script successivo che rigenera il CSV da zero senza controllare le righe provvisorie aggiunte a mano. `CLAUDE.md` vieta di nascondere un conflitto: la causa radice (script di rigenerazione idempotente ma non a conoscenza delle patch successive) va segnalata, non aggirata con un nuovo workaround ad-hoc.
- Conseguenze: `data/staged/fantacalcio_voti_manual/_m3_replacement_values.csv` e `data/local/fantacalcio.duckdb` rigenerati (502 giocatori, +4 rispetto a prima). 444 test totali passano invariati (nessuna regressione). 1 nuovo `AssignmentEvent` scritto per Schmid. Rischio noto non risolto in questa sessione: se `run_m3_replacement_values.py` viene rieseguito di nuovo in futuro senza poi ri-lanciare `add_new_signings.py`, la stessa regressione si ripete — nessuna protezione automatica introdotta, solo documentata qui.
- Approvato da: project owner

### ADR-2026-065 — Profilo comportamentale per squadra dalle liste di preferenza complete (comprese le offerte fallite)

- Data: 2026-08-18
- Stato: approved
- Scope: auction | domain
- Decisione: `scripts/ingest_preference_bid_history.py` cura le 9 liste grezze admin ("Lista N (Risposte).xlsx") in `data/curated/preference_bid_history/preference_bids.csv` — una riga per (squadra, preferenza), con esito da colore cella (verde=vinta, rosso=tentata e persa, arancione=mai valutata), mai un esito indovinato per un colore diverso da questi tre (marcato `unknown`). Lista 1 (Portieri) esclusa da questa esecuzione: usa nomi di club invece di nomi giocatore, formato diverso non ancora gestito dal resolver — nessun dato inventato al suo posto. Sulle 6 liste rimanenti, 616/~770 righe risolte (~80%): i nomi non risolvibili con sicurezza (ambiguità genuina, es. più giocatori omonimi senza contesto per disambiguare) sono esclusi, mai forzati.
  `market_model.team_preference_profiles(history, player_conn)` (pura, nessun I/O) aggrega questo storico per squadra: quante preferenze vinte/perse/mai valutate, rapporto medio offerta/quotazione sulle vinte vs sulle perse, posizione media in lista quando vince — un segnale di aggressività/strategia reale della squadra avversaria, non i soli esiti finali già coperti da `team_aggressiveness_index` (ADR-2026-058, che usa solo il ledger reale = solo le vittorie).
- Rationale: l'utente ha chiesto esplicitamente di modellare "la strategia dalle loro offerte" usando anche le offerte fallite, non solo gli esiti — un segnale che il ledger reale (solo vittorie) non può dare da solo. Confermato con l'utente: uso limitato al profilo comportamentale, mai per inferire domanda futura su un giocatore specifico.
- Conseguenze: 3 nuovi test in `tests/test_auction_market_model.py::TestTeamPreferenceProfiles`. 447 test totali passano. Non ancora integrato in una pagina UI né in `bid_recommendation.py` — resta un modulo dati/dominio pronto per un consumo futuro esplicito, per non introdurre un altro correttivo di prezzo non richiesto (stessa cautela già applicata al segnale di concorrenza in ADR-2026-057). Gap noti: Lista 1 (portieri) non ingerita, ~20% delle righe delle altre 6 liste escluse per nomi ambigui/non risolti.
- Approvato da: project owner

### ADR-2026-066 — Pagina "Buste G3": busta libera senza liste, spesa peggiore = somma (non massimo)

- Data: 2026-08-18
- Stato: approved
- Scope: auction | UX
- Decisione: nuova pagina `app/pages/7_📮_Buste_G3.py` per comporre la busta G3 (fase finale a offerta secca senza liste, già in config come `sealed_bid_free`/`max_players_this_phase`, mai esposta prima in una pagina). A differenza di G2 (fasce, si vince al più 1 per fascia), in G3 ogni offerta è indipendente: fino a `max_players_this_phase` (letto da config, non hardcoded — aggiunto il campo mancante a `config.Round`) giocatori liberi qualsiasi, offerta minima = quotazione effettiva del giocatore (non 1 credito). `g3_envelopes_store.py` (stesso pattern di `g2_envelopes_store.py`, un solo giocatore per riga senza rank di preferenza) + `g3_envelope_feasibility.py`: la spesa nel caso peggiore è la **somma** di tutte le offerte in busta (non il massimo come in G2), perché non c'è alcun cap "al più uno" che protegga da vincerle tutte.
- Rationale: l'utente ha chiesto un'interfaccia per l'ultima fase (busta chiusa senza liste, fino a 6 offerte, potenzialmente 6 vittorie) in parallelo all'ingest dei risultati G2 reali. Riusare interamente il pattern G2 (store, feasibility, pagina) sarebbe stato sbagliato nel punto più importante — il caso peggiore — perché la struttura di rischio è opposta: G2 limita a 1 vittoria per fascia, G3 no.
- Conseguenze: `config.Round` guadagna `max_players_this_phase` (opzionale, default None, letto da YAML se presente) invece di essere ignorato dal loader. 20 nuovi test (4 store, 16 feasibility). 463 test totali passano. Verificato in browser: pagina carica senza errori, budget G3 letto dal ledger reale (40 per team_01), nessun eccezione Streamlit, form di aggiunta presente.
- Approvato da: project owner

### ADR-2026-067 — Simulazione Monte Carlo della competizione avversaria in G3, collegata alla pagina Buste G3

- Data: 2026-08-18
- Stato: approved
- Scope: auction | domain | UX
- Decisione: l'utente ha chiesto di collegare tutti i segnali di mercato già esistenti e di poter "girare delle simulazioni" della fase finale (G3, busta chiusa senza liste) per calibrare le proprie offerte. Metodo concordato esplicitamente con l'utente prima di implementare (mai una formula statistica inventata in silenzio, `CLAUDE.md`):
  1. **Chi potrebbe offrire**: ogni avversario con uno slot di ruolo ancora scoperto è un candidato; la probabilità che punti proprio QUESTO giocatore = (suoi slot scoperti in quel ruolo) / (pool residuo non assegnato di quel ruolo) — la frazione attesa di ciò che resta che finirà per comprare, non un giudizio su "quanto gli piace" questo giocatore specifico (dato che non esiste).
  2. **Quanto offrirebbe**: quotazione effettiva del giocatore × un rapporto di inflazione campionato dal range storico osservato (stessa cascata ruolo/fascia già usata in `bid_recommendation.py`), corretto per lo stile della singola squadra (`team_aggressiveness_index`) quando ci sono abbastanza dati per quella squadra, altrimenti lasciato al livello di lega.
  3. **Incertezza**: campionamento uniforme dentro il range min-max storico già calcolato, mai una forma di distribuzione inventata.
  Nuovo modulo puro `src/fantacalcio/auction/g3_simulation.py`: `simulate_opponent_competition()` esegue 5000 simulazioni (seed di default fisso per riproducibilità, `CLAUDE.md`: simulazione deterministica/seedata) e produce percentili dell'offerta massima avversaria simulata + probabilità di nessuna competizione; `win_probability_for_bid()` risponde "con questa offerta, che probabilità ho di vincere?" sui campioni grezzi, senza dover rilanciare la simulazione per ogni importo. `PriceCorrection` (market_model.py) esteso con `low_ratio`/`high_ratio` (non esposti prima) per alimentare il campionamento. `app/pages/7_📮_Buste_G3.py` mostra la simulazione sia per le offerte già salvate in busta sia in anteprima live mentre si sceglie giocatore/importo, con spiegazione passo-per-passo sempre visibile.
- Rationale: G3 non ha liste, quindi "chi punta chi" è genuinamente sconosciuto — la richiesta di girare simulazioni per orientarsi ha bisogno di un modello esplicito e concordato, non di un numero indovinato. Riusare la stessa cascata di inflazione già validata (ADR-2026-058) invece di inventarne una nuova mantiene un'unica fonte di verità per "quanto si paga sopra quotazione".
- Conseguenze: 4 nuovi test in `tests/test_auction_g3_simulation.py` (determinismo con stesso seed, nessun avversario eleggibile → vittoria certa, probabilità di vittoria crescente con l'offerta, spiegazione che nomina la fonte della correzione). 467 test totali passano. Verificato in browser: simulazione live funzionante sui dati reali (19 avversari eleggibili, offerta avversaria mediana stimata coerente con l'inflazione osservata). Il profilo da preferenze complete (`team_preference_profiles`, ADR-2026-065) resta volutamente NON usato qui: è un segnale ancora parziale (~80% di copertura, Lista 1 esclusa) e usarlo avrebbe richiesto un'altra scelta di combinazione non ancora concordata — `team_aggressiveness_index` (dati ledger reali al 100%, già approvato in ADR-2026-058) resta l'unico segnale di stile-squadra in questa simulazione.
- Approvato da: project owner

### ADR-2026-068 — Audit ingestione lista admin: bug reale (admin_rank azzerato) + 2 giocatori con ruolo forzato in asta

- Data: 2026-08-18
- Stato: approved
- Scope: data-quality | auction
- Decisione: su richiesta dell'utente, verificate le 180 righe di `Liste Fantacalcio 26_27.md` (hash identico al file già ingerito il 2026-08-16, nessun contenuto nuovo) contro `admin_rank`/`list_pool_name` nel player_table. Trovati due problemi reali:
  1. **Bug**: `apply_official_admin_list()` azzerava incondizionatamente `admin_rank`/`admin_score` all'inizio per TUTTE le righe, comprese quelle sintetiche a `player_code` negativo (Molina N., Obrador, Spence, Schmid — `scripts/add_new_signings.py`, ADR-2026-051), che non possono mai comparire in `resolved_players.csv` per costruzione. Conseguenza pratica: questi 4 giocatori, pur `official`, avevano `admin_rank` nullo e fallivano il controllo "admin_rank noto" di `g2_envelope_feasibility.check_pick_feasibility` — non potevano essere aggiunti a nessuna busta G2 nell'app. Corretto preservando `admin_rank`/`admin_score` per le righe a codice negativo attraverso il reset. Il danno era già scritto anche nel CSV sorgente (una precedente esecuzione di `apply_admin_official_list.py`, ADR-2026-064, l'aveva salvato con i valori azzerati) — ripristinati i 4 valori noti direttamente nel CSV, poi ricostruito il DuckDB con la pipeline corretta.
  2. **Non un bug, una scelta di regolamento della lega confermata dall'utente**: Rodriguez Je. e Isaksen sono classificati ruolo `C` (centrocampista) dalle quotazioni fantacalcio ufficiali, ma l'admin della lega li ha inseriti nella lista Attaccanti 21-40 (rank 37/40) "per fare l'asta" — restano centrocampisti per punteggio/rosa, ma vanno pescati tramite la busta Attaccanti. Corretto `list_pool_name` a `forwards_top_21_40` per questi 2 soli `player_code` (7129, 6398), lasciando `role='C'` invariato — così l'assegnazione futura conta ancora come slot centrocampista (`import_g2_results.py` deriva il ruolo di dominio da `player["role"]`, non da `list_pool_name`).
  Nota di provenienza, non corretta retroattivamente: i 2 eventi reali già scritti sul ledger per questi giocatori (team_02/Isaksen, team_17/Rodriguez Je., ADR-2026-063) portano ancora `pool_id="midfielders_top_21_40"` (il valore risolto al momento dell'import, prima di questa correzione) — `role=MID` è comunque corretto e nessun invariante di dominio è violato, quindi non si è ritenuto necessario un evento di correzione per un campo di sola provenienza.
- Rationale: `CLAUDE.md` vieta di nascondere un conflitto scoperto durante un controllo esplicito e di lasciare un bug funzionale attivo senza segnalarlo. Il secondo caso è stato verificato con l'utente invece di essere corretto a intuito, perché la classificazione admin-vs-quotazioni-ufficiali è esattamente il tipo di scelta che il progetto vieta di indovinare.
- Conseguenze: `src/fantacalcio/auction/apply_official_admin_list.py` corretto (preserva admin_rank/admin_score per player_code negativi). 1 nuovo test in `tests/test_auction_apply_official_admin_list.py`. 468 test totali passano. Dati reali corretti: `_m3_replacement_values.csv` e `fantacalcio.duckdb` rigenerati. Verificato: le 180 righe della lista admin ora risolvono tutte correttamente contro il player_table (prima: 6 mismatch).
- Approvato da: project owner

### ADR-2026-069 — Profilo da preferenze complete collegato alla simulazione G3 (cascata con l'aggressività da ledger)

- Data: 2026-08-18
- Stato: approved
- Scope: auction | domain | UX
- Decisione: `team_preference_profiles` (ADR-2026-065, offerte vinte E perse) è ora usato nella simulazione G3 (ADR-2026-067) al posto del solo `team_aggressiveness_index` (solo vittorie dal ledger), con una cascata esplicita in una nuova `market_model.team_price_multiplier()`: se per una squadra ci sono abbastanza osservazioni nel profilo da preferenze (>= 6 tra vinte+perse), usa la media pesata dei rapporti prezzo/quotazione su vinte+perse; altrimenti ricade sull'aggressività da ledger (copertura completa ma solo vittorie); altrimenti nessuna correzione. La fonte usata è sempre nominata nella spiegazione mostrata in UI, mai mescolata silenziosamente. `app/pages/7_📮_Buste_G3.py` carica lo storico curato (`data/curated/preference_bid_history/preference_bids.csv`) una sola volta per sessione via `st.cache_data` (chiave: mtime del CSV) invece di ricalcolare il profilo per tutte le squadre a ogni rerun del widget -- trovato un problema di performance reale in verifica (ogni ricalcolo interroga il DB riga per riga su ~600 righe, diversi secondi), rilevante perché l'uso previsto è sotto pressione durante un'asta dal vivo.
- Rationale: l'utente ha chiesto esplicitamente di collegare questo segnale dopo la prima versione della simulazione G3, che lo aveva lasciato fuori proprio per non introdurre un'altra scelta di combinazione non concordata. La cascata più-specifico-e-affidabile-vince replica lo stesso principio già approvato per l'inflazione di prezzo (`estimate_price_correction`, ADR-2026-058), quindi non è una nuova regola statistica ma un riuso dello stesso pattern.
- Conseguenze: 4 nuovi test in `tests/test_auction_market_model.py::TestTeamPriceMultiplier`, 1 nuovo in `tests/test_auction_g3_simulation.py` (il profilo da preferenze sposta davvero l'offerta avversaria simulata). 473 test totali passano. Verificato in browser sui dati reali: simulazione funzionante end-to-end, spiegazione mostra la fonte del moltiplicatore per ogni avversario eleggibile.
- Approvato da: project owner

### ADR-2026-070 — Engine v2: livelli `features`/`models`, priori da quote, ingest manuale Understat/WhoScored, forecast-to-bid avanzato (ombrello)

- Data: 2026-09-01
- Stato: approved
- Scope: architecture | data | modeling | auction | operations
- Decisione: programma a fasi (M7 "Engine v2" in `docs/ROADMAP.md`) per portare a compimento i livelli 4 (`features`) e 5 (`models`) della tassonomia dati e condizionare il forecast con segnali finora ingeriti ma non collegati. Ogni fase ha il suo ADR di dettaglio (ADR-2026-071+) e il suo gate; questo ADR fissa i vincoli comuni:
  1. **Tutto su branch `feature/engine-v2`.** Il path d'asta live (pagine Buste G2/G3, `auction/g3_*`, `auction/g2_envelope_feasibility.py`, ledger, `domain.py`) è toccato solo in aggiunta e resta verde a ogni commit. **Nessun merge su `fanta` prima dell'ultimo giro extra a busta chiusa.**
  2. **Reversibilità**: ogni nuovo input ha un fallback documentato al comportamento attuale, ed è il default finché un gate OOS (CRPS/coverage/MAE/rank-corr su split rolling-origin) non lo promuove. I risultati di ADR-2026-012/018/026 restano riproducibili.
  3. **Determinismo**: ogni nuova funzione stocastica prende un `rng` esplicito e ha un test "stesso seed → output identico" (come `simulate_fantavoto`).
  4. **Leakage**: ogni feature porta `event_time/available_time/ingested_time/source_version/quality_tier`; ogni fit/backtest aggiunge l'assert `available_time <= decision_time` (generalizzazione di `modeling/validation.assert_no_leakage`).
  5. **Budget dipendenze** (deroga circoscritta alla regola "dipendenze minime" di `CLAUDE.md`, motivata caso per caso): `numpy` reso esplicito in `pyproject.toml` (già transitivo, zero cambi di comportamento); `scikit-learn`+`lightgbm` come extra opzionale `ml`, importati con guardia, promossi in produzione **solo** se battono le baseline obbligatorie OOS; prezzo ombra del budget via Lagrangiano sul DP esistente di `roster_optimizer.py` (**niente OR-Tools**; `PuLP` solo come cross-check nei test, extra `solver`); `socceraction` **non** diventa dipendenza committata — il prototipo VAEP/xT resta uno spike offline in scratchpad i cui risultati confluiscono in un ADR.
  6. **Override d'uso personale per Understat e WhoScored** (vedi sotto).
- Override scraping (deroga esplicita del project owner alla riga "Understat — Escluso" e alla sezione "Fonti non automatizzabili" di `docs/SOURCE_REGISTER.md`, e alla regola "mai scraping di fonti che lo vietano" di `CLAUDE.md`, limitata all'uso personale locale): il project owner autorizza l'uso di dati Understat (xG/npxG/xA/tiri/minuti) e WhoScored (infortuni/squalifiche/probabili formazioni) per il proprio fantacalcio privato. Understat dichiara `robots.txt: Disallow: /`, riconosciuto. Confine tecnico adottato per contenere il rischio:
  - i **parser** (`ingest/understat.py`, `ingest/whoscored.py`) sono puri, senza HTTP, e richiedono un file già presente su disco — identici per pattern a `ingest/fantacalcio_voti.py`;
  - qualsiasi script di **fetch** (`ingest/understat_fetch.py`, `ingest/whoscored_fetch.py`) è standalone (`if __name__ == "__main__"` only), rate-limited (≥5 s), con cache su disco, **mai importato da un modulo di pipeline, da `scripts/run_*` o dalla CI** (test statico che lo verifica);
  - l'HTML/JSON grezzo non viene mai committato (`data/raw|staged` già in `.gitignore`); le fixture di test sono sintetiche;
  - la fonte è progettata sostituibile: se domani si acquista un provider licenziato, si cambia solo il modulo di ingest.
  Se il project owner non vuole nemmeno i parser committati, gli Stage 3/7 si declassano a "spec del formato file in `docs/` + parser in scratchpad".
- Rationale: l'utente ha chiesto di "usare tutto il disponibile" per massimizzare previsione e valutazione d'asta. L'esplorazione ha mostrato che (a) `football-docs` (MCP nutmeg) è solo documentazione — utile come riferimento, non come dato; (b) il valore realistico è completare i livelli `features`/`models` già previsti e collegare le quote football-data.co.uk (già production, `modeling/market_odds.py` le de-vigga già ma non alimentano nulla) e la partecipazione (calcolata ma fuori dalla simulazione); (c) i dati evento della stagione corrente non sono disponibili gratis, quindi VAEP/xT restano benchmark. Un rewrite "a scatola chiusa" è escluso da `CLAUDE.md` (minima modifica coerente, niente conflitti nascosti): da qui il programma a fasi gated e reversibili.
- Conseguenze: nuovi package `src/fantacalcio/features/` e `src/fantacalcio/models/`; nuove cartelle gitignored `data/features/`, `data/models/`; `pyproject.toml` guadagna `numpy` esplicito e gli extra `ml`/`solver`; `.mcp.json` diventa gitignored (path Windows assoluto non portabile) con copia locale `.mcp.local.json` e nota di setup in `CLAUDE.md`. `docs/ROADMAP.md` guadagna M7. `docs/SOURCE_REGISTER.md` guadagna la sottosezione "Override d'uso personale". 2 nuovi test in Stage 0 (`tests/test_mcp_config.py`, `tests/test_project_metadata.py`); 473 test esistenti restano verdi. Nessun cambio di comportamento in Stage 0.
- Approvato da: project owner

### ADR-2026-071 — Engine v2 Stage 1: livello `features/` (tassonomia livello 4) e `modeling/metrics.py`

- Data: 2026-09-01
- Stato: approved
- Supersedes: nulla (implementa il livello 4 previsto da `docs/DATA_AND_MODELING.md` §"Livelli dati", finora non costruito)
- Scope: architecture | data | modeling
- Decisione: nuovo package `src/fantacalcio/features/` — feature store long-format (`entity_type, entity_id, season, feature_name, value` + 7 colonne di lineage: `event_time, available_time, ingested_time, source_name, source_version, quality_tier, quality_status`), persistenza su `data/features/<dataset>/data.parquet` via `COPY ... (FORMAT parquet)` di DuckDB (niente `pyarrow`; fallback gzip-CSV con motivo loggato), lettura con slicing `as_of` (`available_time <= cutoff`) e filtro per nome. `schema.py` valida ogni frame contro un `FEATURE_REGISTRY` di 18 feature. `leakage.py` generalizza `modeling/validation.assert_no_leakage` alla forma long (`assert_available_before_decision`, versione batch sui fold). `build.py`: 6 builder deterministici che **chiamano** i moduli esistenti senza riscriverne la matematica (`player_voto.walk_forward`, `participation`, `time_decay`, `dixon_coles`, `elo`, `fvm_prior`, listone) e materializzano nel formato. `modeling/metrics.py`: metriche condivise (CRPS energy-form con 0 esatto per point-mass, PIT/coverage, Brier, log-loss, NDCG@k, MAE/RMSE, Spearman); `modeling/validation.py` re-importa `log_loss` da qui (shim retrocompatibile, firma invariata). `scripts/build_features.py` è l'entrypoint batch.
- Rationale: senza il livello 4 ogni stage successivo di Engine v2 (priori da quote, xG, boosting, forecast-to-bid) dovrebbe reinventare lineage e controllo di leakage. È infrastruttura, nessuna scelta statistica nuova.
- Conseguenze: 45 nuovi test (metrics, schema, store, leakage, build), 518 totali passano. Approssimazioni dichiarate, da raffinare in stage successivi: `available_time` per le feature per-giornata è sintetico (inizio stagione + 7 giorni × (giornata−1)), deterministico e monotono ma non le date reali dei turni Serie A; `build_team_strength_features` usa `k_factor`/`home_advantage` di default espliciti, non necessariamente allineati a un run Elo canonico (nessun regression-lock su questa feature, solo su `voto_running_shrunk_mean`); i path in `store.py` sono interpolati in SQL (rischio nullo: nomi dataset controllati dal codice). Nessun path a valle modificato (`run_monte_carlo_fantavoto.py`, `build_player_table.py`, pagine invariati); il feature store non alimenta ancora nulla — lo farà da Stage 2.
- Approvato da: project owner

### ADR-2026-074 — Engine v2 Stage 2: priori da quote nel Monte Carlo

- Data: 2026-09-02
- Stato: approved (feature costruita, **default OFF**: ship gate OOS non superato su dati reali)
- Supersedes: nulla; affianca ADR-2026-023 (`team_strength_adjustment`, che resta il fallback quote-assenti)
- Scope: modeling | scoring
- Prior art: `docs/research/priorart_stage2.md` (non vincolante), §8 la raccomandazione.
- Decisione:
  - **De-vig**: `modeling/odds_priors.devig(odds, method="shin")` — Shin (1993), solve 1-D per `z` su `[0, 0.2]` (Brent), forma chiusa additiva per il caso 2-esiti (O/U); `method="multiplicative"` come fallback e cross-check (generalizza `market_odds.implied_probabilities` a n esiti), `method="power"` opzionale. `shin_z()` esposto come segnale di qualità dato (rigetta righe con `z` fuori `[0, 0.15]`). Input primario = mercato medio (`Avg*` → `BbAv*` → `B365*`, resolver stagione-aware).
  - **Modello gol**: `team_goals_distribution()` — parametrizzazione supremacy+total. `T` fissato da `P(Over 2.5)` (inversione tail di Poisson 1-D) o da `total_goals`; se nessuno dei due è dato → `ValueError` (il chiamante passa una media stagionale). `s` fissato per root-find bracketato (Brent) che pareggia `p_home − p_away` sulla griglia scoreline **Dixon–Coles** con `rho = −0.08` fisso. Fallback `method="independent"` (Poisson indipendente, `rho = 0`) quando il solve DC non fa bracket — path loggato. Sanity gate §2f: `T ∈ [0.7, 4.5]` (fuori → `ValueError`), `|s| ≤ 2.5` (fuori → clamp+warning), massa griglia ≈ 1.
  - **Clean-sheet / gol subiti**: `clean_sheet_prob` / `goals_conceded_pmf` / `expected_goals_conceded` / `match_outcome_probs` dal **grid congiunto**. Nota onesta rilevata in implementazione: la correzione `tau` di Dixon–Coles lascia **entrambe le marginali di gol esattamente invariate** (i termini `tau` si cancellano: `P(Y=0) = e^{−λ_A}` esatto prima della rinormalizzazione); `rho` sposta massa solo sulla **diagonale** (pareggi) del congiunto, quindi conta per `match_outcome_probs`/supremacy/punti attesi, non per il pmf dei gol subiti. Il condizionamento sul conceded marginal è di fatto indifferente a DC vs Poisson indipendente.
  - **Condizionamento**: `scoring/odds_conditioning.condition_samples()` — importance reweighting/SIR sul marginale `team_goals_conceded` realizzato dell'ensemble bootstrap (per-draw rows via nuovo `simulate_fantavoto(collect_rows=True)`, additivo, default invariato). Pesi `w_s ∝ target_pmf[g_s] / emp_pmf[g_s]`, normalizzati a media 1, **clip a 10× la media**, `temper` opzionale, rinormalizzati, poi resample SIR con `rng` esplicito. ESS loggata; **ESS < 10% di n → ritorna `result` invariato con warning** (nessuna degradazione silenziosa); pesi degeneri (somma 0) → invariato. Per A/C: `scale_scoring_propensity()` scala la frequenza dei draw con gol/assist per il rapporto gol-squadra implicito dalle quote (resample, non scalare additivo).
  - **Metriche**: aggiunte `metrics.crps_fair` (stimatore unbiased `1/(m(m−1))`, Zamo–Naveau 2018) e `metrics.rank_histogram` (Talagrand).
  - **Wire-in**: `scripts/run_monte_carlo_fantavoto.py --odds-priors` (default off → output byte-identico; con flag, la stagione 2026/27 non è prezzata → logga la degradazione e usa lo shift scalare). Backtest `scripts/run_stage2_odds_backtest.py` (rolling-origin 2022/23–2025/26, 3 arm: scalare / quote / nessuno). Feature registrate: `team_odds_clean_sheet_rate`, `team_odds_expected_goals_conceded`, `team_odds_expected_points` (`features/schema.py`) + builder `build_odds_prior_features` (`features/build.py`).
  - **Ship gate**: promuovere a default ON solo se le priori-da-quote battono lo shift scalare su `CRPS_fair` per P **e** D e sul Brier clean-sheet nel walk-forward, senza regressione PIT/coverage, con ESS sana sul grosso delle fixture.
- Risultati backtest (dati staged, 500 sim/giocatore, seed 42, target = fantamedia stagionale — approssimazione season-aggregate vs ensemble di singola giornata):
  - CRPS_fair medio: D 0.4291 (quote) vs 0.4351 (scalare) vs 0.4342 (nessuno) — **quote meglio**; C 0.5209 vs 0.5241 vs 0.5222 — **quote meglio**; A 0.7364 vs 0.7417 vs 0.7444 — **quote meglio**; P 1.7322 vs 1.6740 vs 1.6740 — **quote peggio** (regressione).
  - Brier clean-sheet (CS-rate implicita dalle quote vs realizzata, 80 team-stagione): 0.0058 vs 0.0141 baseline base-rate.
  - Coverage P10–P90 arm quote: P 0.67, D 0.93, C 0.93, A 0.95; PIT mean P 0.31 (portieri sotto-coperti/biased), D 0.51.
  - **Ship gate: FAIL** — regressione su P (target conceded-pmf Poisson grezzo inadatto ai portieri, la cui varianza fantavoto è dominata da parate/rigori, non dai gol subiti di squadra). Default resta **OFF**.
- Caveat onesto: il miglioramento riguarda forecast rolling/settimanali e i backtest su stagioni concluse e prezzate. **Non** migliora il numero stagionale pre-asta 2026/27: non esistono fixture prezzate per una stagione non iniziata → su quel path la degradazione allo shift scalare è la sola opzione.
- Conseguenze: nuovi `src/fantacalcio/modeling/odds_priors.py`, `src/fantacalcio/scoring/odds_conditioning.py`; `metrics.py` +2 funzioni; `monte_carlo.simulate_fantavoto` +param `collect_rows` (default False, comportamento invariato); `features/schema.py` +3 feature, `features/build.py` +1 builder; 2 nuovi script `run_*`. 29 nuovi test, **547 totali passano**. Nessun path d'asta live toccato. Nessun merge su `fanta`.
- Approvato da: project owner

### ADR-2026-075 — Engine v2 Stage 3: ingest manuale Understat + feature xG/xA + propensione gol/assist xG-informata nel Monte Carlo

- Data: 2026-09-02
- Stato: approved (feature costruita, **default OFF**; gate deferito — non eseguibile senza export Understat reali)
- Supersedes: nulla; estende ADR-2026-070 (override d'uso personale) e affianca ADR-2026-074 (Stage 2, di cui è la baseline di degradazione)
- Scope: data | modeling | scoring
- Decisione:
  - **Parser puro** `src/fantacalcio/ingest/understat.py` — zero HTTP, richiede un file già su disco (JSON decodificato o pagina HTML con blob `var ... = JSON.parse('...')` a escape `\xHH`; supportati entrambi). `parse_player_season()` → aggregati stagione per giocatore (`games, time, goals, xG, assists, xA, shots, key_passes, npg, npxG, xGChain, xGBuildup`), `parse_shot_events()` → tiri (`minute, x, y, xG, result, situation, shotType, player, a_player`), best-effort. `StagedUnderstat` frozen: frame + `file_sha256`, `source_name="understat"`, `quality_tier="C"`, `available_time` = fine stagione (30 giugno dell'anno dopo) per gli aggregati / data ultimo tiro per gli eventi. `UnderstatParseError(ValueError)`; mai dati inventati, raise su struttura inattesa. **Assunzioni di formato dichiarate nel docstring** (nessun sample reale disponibile).
  - **Fetch standalone** `src/fantacalcio/ingest/understat_fetch.py` — `if __name__ == "__main__"` only, nessun fetch a livello modulo, rate-limit ≥5 s, cache su disco, snapshot via `snapshot.write_snapshot`. Docstring: override uso personale, `robots.txt: Disallow: /` riconosciuto, ADR-2026-070, "non parte della pipeline automatica". `tests/test_ingest_understat.py` verifica staticamente che nessun `scripts/run_*` e nessun modulo `fantacalcio/**` lo importi.
  - **Batch** `scripts/ingest_understat_folder.py` — parse di una cartella di file salvati a mano → `data/staged/understat/` + manifest (come `ingest_listone_folder.py`).
  - **Feature** `src/fantacalcio/features/xg_features.py` — per-90 shrinkati dagli aggregati stagione: `xg_per90_shrunk, npxg_per90_shrunk, xa_per90_shrunk, shots_per90_shrunk, minutes_understat`, + `xg_overperformance_shrunk` = `(gol − xG)/90` shrinkato **fortemente verso 0**. Shrinkage `w = n/(n+prior)` come `player_voto.shrunk_estimate` (n = tiri, target = media per-90 di ruolo). Join a `player_code` **solo** via `identity/player_name_resolver` (role-constrained); non risolti/omonimi stesso ruolo → lista review-queue restituita, mai un codice indovinato. Lineage completo, tier C, `as_of` per stagione. Registrate in `features/schema.py`; `build_xg_features` esportata da `features/build.py` (+ arg opzionali `understat_season`/`understat_anchor_players` in `build_all_features`).
  - **Condizionamento MC** `src/fantacalcio/scoring/xg_propensity.py::adjust_event_propensity(...)` — fonde tasso gol/assist storico realizzato e tasso implicito da xG/xA con `w = n/(n+prior_events)` (n = eventi realizzati nelle righe campionate), poi SIR-resample dell'ensemble (upweight dei draw con gol/assist per il rapporto blended/storico, clip, ESS floor 10%, invariato se degenere). **Assente xG (`xg_goal_rate is None`) → peso 0 → ritorna `result` invariato e byte-identico** (contratto di degradazione controllata).
  - **Wire-in** `scripts/run_monte_carlo_fantavoto.py --xg` (default OFF): quando `data/staged/understat/` è popolato, instrada la propensione gol/assist A/C per `xg_propensity`; assente → no-op. Colonna di provenienza `xg_data_present` sempre scritta.
- Gate (deferito): promuovere a default ON solo se, in backtest walk-forward, il MC con xG batte il MC senza xG su CRPS gol/assist per A/C senza regressione altrove. **Non eseguibile ora** (nessun export Understat reale); il wiring è spedito absent-safe e OFF, il risultato del gate sarà messo a verbale quando ci saranno dati reali.
- Degradazione: assenza di dati Understat → path bit-identico allo Stage 2 (verificato da `tests/test_scoring_xg_propensity.py::test_absent_xg_returns_input_byte_identical` e dal contratto `--xg` no-op).
- Conflitto noto risolto: `CLAUDE.md` "mai scraping di fonti che lo vietano" + `SOURCE_REGISTER` "Understat — Escluso" → ADR-2026-070 (override uso personale) + riga template Understat compilata in `docs/SOURCE_REGISTER.md`, parser puro/offline, fetch fuori pipeline/CI, HTML mai committato, fixture sintetiche.
- Conseguenze: nuovi `src/fantacalcio/ingest/understat.py`, `src/fantacalcio/ingest/understat_fetch.py`, `src/fantacalcio/features/xg_features.py`, `src/fantacalcio/scoring/xg_propensity.py`, `scripts/ingest_understat_folder.py`; `features/schema.py` +6 feature (tier C), `features/build.py` +re-export/+dispatch; `scripts/run_monte_carlo_fantavoto.py` +`--xg` +colonna `xg_data_present`. 20 nuovi test (`tests/test_ingest_understat.py`, `tests/test_features_xg.py`, `tests/test_scoring_xg_propensity.py`, +`tests/test_features_build.py`), **567 totali passano**. Nessun path d'asta live toccato. Nessun merge su `fanta`.
- Approvato da: project owner

### ADR-2026-076 — Engine v2 Stage 6: forecast-to-bid (prezzo ombra del budget, covarianza delle scelte, domanda avversaria nel prezzo, profilo di rischio)

- Data: 2026-09-02
- Stato: approved (feature costruita; il path di default resta **byte-identico** a oggi, provato da regression test)
- Supersedes: nulla; **inverte parzialmente** ADR-2026-057 (segnale di concorrenza "riportato, non moltiplicato") in modo **opt-in** (vedi sotto)
- Scope: modeling | auction | architecture
- Prior art: `docs/research/priorart_stage6.md` (non vincolante), §7 la raccomandazione.
- Decisione:
  - **Prezzo ombra del budget** — `src/fantacalcio/auction/budget_shadow_price.py`. Rilassamento Lagrangiano del knapsack DP **esistente** (`roster_optimizer.optimize_roster_completion`) dualizzando **solo** la riga di budget: il problema interno si decompone per ruolo ("prendi i top-`k_r` per valore ridotto `VAR_i − λ·prezzo_i`", conteggi di ruolo come uguaglianze → si prendono i meno negativi se servono). Bisezione (≤200 iter, `tol=1e-4`) sullo slack di budget monotono `g(λ) = budget − costo(λ)` per il più piccolo `λ*` la cui scelta ottima rientra. `ShadowPriceResult` frozen: `lambda_star` (VAR marginale per credito), `binding: bool` (`False` + `lambda_star=0.0` quando la scelta a `λ=0` già rientra — nessun costo-opportunità), `implied_roster: tuple[int,...]`, `duality_gap_estimate: float` (`max(0, L(λ*) − V_IP(DP))`, il gap di interezza del knapsack, da mostrare come confidenza debole). `max_bid_from_shadow_price(player_var, lambda_star, floor=1)` = `ceil(value/lambda_star)`, `≥ floor`; con `lambda_star == 0` ritorna `LARGE_SENTINEL_BID` (int grande **finito**, non `math.inf`) così che siano i cap di budget/prezzo del chiamante a legare. **Nessun solver LP sul path di produzione**: `_pulp_dual_crosscheck` è dietro `try: import pulp` e usato **solo** da un test `@pytest.mark.skipif(not _has_pulp)` (extra `solver`, mai runtime).
  - **Covarianza/complementarità delle scelte** — `src/fantacalcio/auction/pick_covariance.py`. `roster_point_samples` (somma dei vettori-campione allineati), `covariance_matrix` (covarianza campionaria; docstring dichiara che il MC indipendente per-giocatore di oggi la rende diagonale e **sottostima** la correlazione cross-giocatore reale), `marginal_variance_contribution`, `marginal_downside_contribution` (variazione del P`q` della rosa), `complementarity_adjustment(candidate_var, ΔVar, ΔP_q, risk_aversion=0.0)` con `risk_aversion=0 → candidate_var invariato`. **Caveat esplicito: è un floor** finché non arrivano le joint sims dello Stage 4.
  - **Profilo di rischio** — `src/fantacalcio/auction/risk_profile.py`. `cvar(samples, alpha=0.10)` (CVaR di coda inferiore, forma Rockafellar–Uryasev `VaR − (1/α)·E[(VaR − x)^+]`), `risk_adjusted_objective(mean, cvar_value, risk_aversion) = (1−ρ)·mean + ρ·cvar_value`, `ρ ∈ [0,1]`, `ρ=0 → mean` (riproduce **esattamente** il massimizzatore VAR odierno). Helper scalari puri; integrazione nell'ottimizzatore via valore per-candidato aggiustato.
  - **Domanda avversaria → prezzo di aggiudicazione** — `src/fantacalcio/auction/opponent_demand_price.py`. `expected_clearing_price(q, demand_pressure, role_inflation=1.0, g_max=2.5, kappa=0.6, d0=0.0, richest_rival_bid=None)` = forma **limitata e saturante** `q·role_inflation·(1 + (g_max−1)(1 − e^{−κ·max(D−d0,0)}))`, poi `min(..., richest_rival_bid)`. Garanzie: monotona non decrescente in `D`, `≥ q·role_inflation` (assente un clip più basso), finita, `D=0 → q·role_inflation`. `demand_pressure_from_state(league_state, ruleset, role, exclude_team_id, round_id=None, aggressiveness=None)` = somma sugli avversari che hanno ancora lo slot di ruolo scoperto, pesati per budget-per-slot relativo (rinormalizzato a media 1 → domanda uniforme = numero di avversari) e per `team_aggressiveness_index` quando passato; da stato `replay`, leakage-safe. `calibrate_clearing_form(realised_prices_df=None)` = fit pinball/LS opzionale (numpy/scipy), ritorna `DEFAULT_CLEARING_FORM` su frame assente/vuoto o fallimento — convenienza, non sul path.
  - **Integrazione** — `src/fantacalcio/auction/bid_recommendation.py::recommend_max_bid` guadagna `shadow_price=None`, `risk_aversion=0.0`, `opponent_demand=None`, `clearing_form_params=None` (+ `target_player_var_samples=None`, `risk_cvar_alpha=0.10` per il blend media→CVaR). **Tutti opzionali; tutti `None`/`0` → output byte-identico a oggi** (regression test su più casi). Quando presenti: `max_bid = min( VAR_term/λ* (o valore regola-$1 se non binding), prezzo_di_aggiudicazione + margine `CLEARING_BUFFER=1`, budget residuo )`, `≥ 1`. `MaxBidRecommendation` guadagna `shadow_price_ceiling`, `clearing_price_ceiling`, `binding_constraint: str` (default `None`/`""`) e righe di spiegazione in italiano che nominano la fonte di ogni segnale. `roster_optimizer.optimize_roster_completion` guadagna `value_col: str = "var_mean"` + hook `value_fn` (DP invariato; default riproduce il comportamento attuale).
  - **Reversal opt-in di ADR-2026-057** — incorporare la domanda avversaria **nel** prezzo (non solo riportarla a fianco) è una decisione di strategia che ADR-2026-057 aveva lasciato fuori in attesa di regola approvata. Qui il **meccanismo** è costruito ma **opt-in e OFF di default**: `opponent_demand=None` = comportamento vecchio (segnale di concorrenza solo informativo). **La decisione di abilitarlo davvero (passare `opponent_demand=` in produzione, ribaltando ADR-2026-057) resta APERTA e richiede il sign-off esplicito del project owner** — da registrare qui quando dato. Finché non arriva, ADR-2026-057 resta in vigore nei fatti.
- Conseguenze: 4 nuovi moduli `src/fantacalcio/auction/{budget_shadow_price,pick_covariance,risk_profile,opponent_demand_price}.py`; `bid_recommendation.py` e `roster_optimizer.py` estesi in modo additivo. 4 nuovi file di test + estensioni a `tests/test_auction_bid_recommendation.py` / `tests/test_auction_roster_optimizer.py`; **609 test totali passano, 1 skipped** (il cross-check PuLP: `pulp` non installato in questo ambiente — l'extra `solver` lo abilita). Nessun path d'asta live/ledger toccato. Nessun merge su `fanta`.
- Approvato da: project owner per la **costruzione** del meccanismo (piano "Engine v2" approvato). L'**abilitazione** della domanda avversaria nel prezzo (reversal di ADR-2026-057) è ancora da approvare esplicitamente.
